# CONS-20260610-003.md
# FID-VN-009 Review — Hybrid NER: PhoBERT + Rule-based (L1c)
# MediVoice VN | 2026-06-10 | Author: Claude Sonnet 4.6
# Status: CLOSED — APPROVE_WITH_CHANGES | ChatGPT + Grok + Copilot | 2026-06-10

---

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSULTATION REQUEST [CONS-20260610-003]
MediVoice VN — AI voice scribe cho phòng mạch tư VN
Topic: FID Review — Hybrid NER Architecture (PhoBERT + Rule-based)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PRODUCT CONTEXT

MediVoice VN: BS nói → PhoWhisper ASR → Drug Correction (L1b v2 DONE) →
NER (L1c) → Mẫu bệnh án 15/BV-01 → BS approve → PDF
100% offline · phòng mạch tư VN · CPU i5-12400F, 32GB RAM, không GPU

## CURRENT STATE

L1c hiện tại = rule-based only (446 LOC, regex patterns):
  ✅ Sinh hiệu: nhiet_do, huyet_ap, mach, nhip_tho, can_nang, spo2
  ✅ Chan_doan: keyword-triggered diagnosis extraction
  ✅ Don_thuoc: drug + dose + frequency (kết hợp với L1b DrugCorrectionEngine v2)
  ⚠️ Trieu_chung: chỉ bắt keyword cố định (miss contextual symptoms)
  ⚠️ Tai_kham: miss variant phrases ("theo dõi thêm", "gặp lại sau")
  ⚠️ Ly_do: miss implicit chief complaints

TRAIN-002 (DONE 2026-06-10): PhoBERT NER trained trên 10,000 synthetic VN sentences
  F1=99.44% (synthetic) | entities: MEDICATION/SYMPTOM/VITAL/FOLLOWUP/DOSE/FREQUENCY/DURATION
  Model: models/ner_phobert/best/ (512MB)
  NOTE: Synthetic only — real-world F1 unknown (pilot audio BENCH-002b chưa có)

## FID-VN-009 DESIGN (Claude đề xuất)

### Architecture: PARALLEL HYBRID

transcript
    ├── Rule-based NER (current)  → MedicalEntities_rule
    ├── PhoBERT NER (new)         → MedicalEntities_phobert
    └── MERGE                     → MedicalEntities_final

MERGE STRATEGY:
  Vitals (nhiet_do, huyet_ap, mach...): rule-based ALWAYS wins
  chan_doan:  rule-based wins nếu non-empty; PhoBERT fills empty
  don_thuoc:  UNION, deduplicate by INN
  trieu_chung: UNION cả hai (PhoBERT bổ sung rule-based)
  tai_kham:  rule-based wins nếu non-empty; PhoBERT fills empty
  ly_do:     rule-based wins nếu non-empty; PhoBERT fills empty

### Default: PhoBERT OFF (opt-in)

USE_PHOBERT_NER = os.getenv("MEDIVOICE_PHOBERT_NER", "false") == "true"
# Lý do: F1 99.44% là synthetic — validate BENCH-002b pilot trước khi bật production

### API Design

# Unchanged backward compat:
extract_entities(transcript: str) -> MedicalEntities

# New hybrid:
extract_entities_hybrid(
    transcript: str,
    use_phobert: bool = False,
) -> tuple[MedicalEntities, dict]

# meta dict:
{
    "rule_fields_filled": [...],
    "phobert_fields_added": [...],
    "phobert_confidence_avg": float,
    "phobert_used": bool,
}

### PhoBERT Inference (lazy load)

@lru_cache(maxsize=1)
def _load_phobert_pipeline():
    from transformers import pipeline
    return pipeline(
        "token-classification",
        model="models/ner_phobert/best",
        aggregation_strategy="simple",
        device=-1,  # CPU always
    )

### BIO → MedicalEntities mapping

PhoBERT entity → MedicalEntities field:
  MEDICATION  → don_thuoc[].inn (if L1b missed)
  DOSE        → don_thuoc[].ham_luong
  FREQUENCY   → don_thuoc[].lieu_dung
  DURATION    → don_thuoc[].so_ngay
  SYMPTOM     → trieu_chung (APPEND)
  VITAL       → SKIP (rule-based wins always)
  FOLLOWUP    → tai_kham (fill if empty)

## 5 CÂU HỎI CỤ THỂ

Q1. PARALLEL vs CASCADE — lựa chọn đúng?

Claude chọn PARALLEL (chạy cả hai, merge sau).
Alternative là CASCADE: rule-based → nếu confidence thấp → chạy PhoBERT.
  Pro cascade: tiết kiệm latency khi rule-based đã đủ tốt
  Con cascade: cần định nghĩa "confidence" cho rule-based (hiện không có)

Bạn đồng ý PARALLEL không? Hay CASCADE tốt hơn cho production medical NLP?

Q2. VITAL entity — nên skip hoàn toàn?

PhoBERT được train để nhận diện VITAL (ví dụ: "mạch 80", "huyết áp 130/80").
Rule-based hiện tại xử lý vitals rất tốt (regex chính xác, nhiều variant).
Claude quyết định: luôn SKIP PhoBERT VITAL → rule-based wins.

Rủi ro: PhoBERT có thể bắt format sinh hiệu mới mà rule-based miss.
Bạn có đồng ý skip VITAL hoàn toàn không?

Q3. MEDICATION entity từ PhoBERT — có conflict với L1b không?

Pipeline: L1b DrugCorrectionEngine v2 → chuẩn hóa drug names về INN → L1c nhận transcript đã chuẩn hóa.
Nếu L1b đã map "am lo" → "Amlodipine", thì PhoBERT L1c sẽ nhìn thấy "Amlodipine" trong transcript.
PhoBERT được train trên sentences có INN drug names → nên nhận được "Amlodipine" correctly.

Câu hỏi: có nên disable MEDICATION entity từ PhoBERT hoàn toàn, vì L1b đã handle?
Hay dùng PhoBERT MEDICATION để VALIDATE L1b output (cross-check)?

Q4. Confidence threshold cho PhoBERT output?

Khi PhoBERT predict entity với score < threshold → nên bỏ qua hoặc flag?
Claude chưa đặt threshold rõ ràng trong FID hiện tại.
Ví dụ: PhoBERT score=0.65 cho SYMPTOM "ho nhẹ" → có nên accept không?

Đề xuất threshold nào? Và có nên confidence-weight khi merge không?

Q5. DEPLOYMENT TIMING — khi nào bật PhoBERT?

Hiện Claude đề xuất: default OFF, bật sau BENCH-002b pilot (real audio).
Nhưng chưa có metrics rõ ràng để quyết định "bật":
  - F1 ≥ X% trên pilot audio?
  - BS review satisfaction rate ≥ Y%?
  - Trieu_chung recall improvement ≥ Z%?

Bạn đề xuất criteria cụ thể để quyết định GO/NO-GO cho PhoBERT production?

## OUTPUT MONG MUỐN

1. ARCHITECTURE: PARALLEL vs CASCADE — lý do
2. VITAL_DECISION: skip hoàn toàn hay partial override?
3. MEDICATION_ROLE: disable / validate / supplement?
4. CONFIDENCE_THRESHOLD: giá trị đề xuất (0.70? 0.80?) và weighting strategy
5. GO_CRITERIA: metrics cụ thể để enable PhoBERT production
6. RISKS: risks nào Claude đã miss?
7. VERDICT: APPROVE / APPROVE_WITH_CHANGES / REJECT FID-VN-009
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## AI RESPONSES (paste vào đây)

### ChatGPT response:
```
VERDICT: APPROVE_WITH_CHANGES

1. ARCHITECTURE: CASCADE > PARALLEL
   Rule-based (primary) → confidence check → PhoBERT (selective fallback) → merge
   Reason: 70-85% cases không cần PhoBERT; PARALLEL wastes CPU + complicates conflict resolution

2. VITAL_DECISION: SKIP hoàn toàn (đồng ý)
   Rule-based VITAL = deterministic truth. PhoBERT VITAL disabled permanently.
   Risk: PhoBERT có thể hallucinate format sinh hiệu ("130/80" → split entity sai)

3. MEDICATION_ROLE: VALIDATE + SUPPLEMENT (không disable, không primary)
   - L1b = primary source
   - PhoBERT = validation + fallback + anomaly detection
   Use case: L1b says Amlodipine, PhoBERT sees Amiodarone pattern → FLAG
   Also: backup coverage for rare drug variants L1b missed

4. CONFIDENCE_THRESHOLD: 2-level system
   confidence ≥ 0.85  → AUTO ACCEPT
   0.60 ≤ score < 0.85 → FLAG for merge + optional human review
   score < 0.60        → DISCARD PhoBERT entity
   Weighting: final_score = 0.7 * rule_confidence + 0.3 * phobert_confidence

5. GO_CRITERIA (real audio only, không dùng synthetic F1):
   SYMPTOM recall ≥ 92%
   MEDICATION recall ≥ 95%
   VITAL accuracy ≥ 99.5%
   BS correction rate ≤ 10%
   Rule vs PhoBERT disagreement ≤ 15%
   GO: recall improves ≥ 5% vs rule-only AND no increase in critical errors

6. RISKS MISSED:
   R-009-07: Error propagation L1b→L1c — if L1b wrong, PhoBERT sees wrong INN → cascade error
   R-009-08: Merge conflict ambiguity — rule:"ho" vs PhoBERT:"ho khan" → need semantic merge, not blind union
   R-009-09: CPU inference bottleneck — 512MB model, need batch inference strategy
   R-009-10: Entity duplication (MEDICATION in L1b + PhoBERT) → dedup logic needs to be stricter

FINAL ARCHITECTURE (production):
  Transcript → L1b Drug Correction → Rule-based NER → [gap/low-conf?] → PhoBERT → Merge Engine → Safety validator
```

### Copilot response:
```
VERDICT: APPROVE_WITH_CHANGES

1. ARCHITECTURE: PARALLEL (không phải CASCADE)
   Lý do: Rule-based không có confidence → cascade trigger không có cơ sở; medical high-stakes cần 2 "con mắt".
   CASCADE chỉ hợp lý khi có coverage score / confidence model cho rule-based (tương lai).
   Latency vài trăm ms chấp nhận được cho phòng mạch tư.

2. VITAL_DECISION: SKIP cho chart — nhưng LOG vào meta (không dùng để điền)
   Rule-based VITAL = production source of truth
   PhoBERT VITAL → log vào meta["phobert_vital_detected"] để phân tích, cải thiện regex sau.
   Không dùng trực tiếp vào MedicalEntities.

3. MEDICATION_ROLE: VALIDATE + SUPPLEMENT
   L1b primary; PhoBERT MEDICATION:
   - Confirm: PhoBERT thấy cùng INN → tăng confidence
   - Supplement: L1b miss → thêm với flagged_for_review=True
   Merge: UNION(L1b, PhoBERT), L1b INN ưu tiên khi conflict.

4. CONFIDENCE_THRESHOLD:
   ≥ 0.80 → accept bình thường
   0.65 ≤ score < 0.80 → flagged_for_review=True
   < 0.65 → discard
   Weighting: không cần complex — chỉ cần phobert_confidence_avg trong meta.

5. GO_CRITERIA:
   F1 (SYMPTOM/FOLLOWUP/MEDICATION) ≥ 0.85 trên real pilot audio (≥50 transcripts)
   SYMPTOM recall ≥ +20% vs rule-only
   BS satisfaction ≥ 80% ("triệu chứng đúng và hữu ích, không thêm thuốc sai")
   Critical errors (medication add mà BS không nói) = 0

6. RISKS ADDITIONAL:
   R-009-11: Symptom over-extraction — PhoBERT tag mọi thứ → chart rối
     → Filter: chỉ giữ symptom liên quan diagnosis context
   R-009-12: FOLLOWUP misinterpretation — "nếu không đỡ thì tái khám" (conditional)
     → PhoBERT tag FOLLOWUP → nhưng BS chưa quyết định
     → Cần phân biệt: definite vs conditional follow-up
   R-009-13: Synthetic over-training → fine-tune trên PhoWhisper output thực sau pilot
```

### Grok response (nếu có):
```
VERDICT: APPROVE_WITH_CHANGES

1. ARCHITECTURE: PARALLEL + smart early-exit (không phải pure cascade)
   Run both → but skip PhoBERT if Rule covers >85% key fields (vitals + diagnosis + drugs)
   Reason: Medical domain không chấp nhận miss; latency 150-400ms CPU acceptable cho batch=1
   Smart early-exit = hybrid approach: accuracy first, latency optimization second

2. VITAL_DECISION: SKIP (đồng ý) — chỉ fallback nếu rule-based detect NOTHING
   PhoBERT synthetic dễ hallucinate format số chữ → rủi ro cao

3. MEDICATION_ROLE: VALIDATE + SUPPLEMENT
   L1b primary; PhoBERT: backup coverage + cross-validation + dose/freq/duration supplement
   Priority: L1b INN > PhoBERT INN khi conflict

4. CONFIDENCE_THRESHOLD:
   MEDICATION, DOSE, FREQUENCY, DURATION → threshold ≥ 0.85
   SYMPTOM, FOLLOWUP → threshold ≥ 0.75
   Discard: score < 0.60

5. GO_CRITERIA (real audio, ≥50 transcripts BENCH-002b):
   F1 ≥ 92% overall (real pilot audio)
   trieu_chung recall improvement ≥ 25% vs rule-only
   False positive rate ≤ 8%
   BS satisfaction ≥ 90% (survey sau 20 ca)
   Latency increase < 600ms vs baseline
   Critical error = 0 tolerance
   Decision gate: A/B test rule-only vs hybrid trên 30-50 ca thực tế

6. RISKS ADDITIONAL:
   Model drift — PhoBERT degrade trên real accent/noise Đà Nẵng → retrain sau pilot
   Over-reliance risk — BS tin output quá → giữ visible confidence + flagged items
   Memory/CPU spike — lazy load + quantization 8-bit nếu cần
   Merge conflict inconsistency — log meta rõ ràng, unit test kỹ
```

---

## SYNTHESIS SECTION (Claude điền sau khi Andy paste response)

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSULTATION SYNTHESIS [CONS-20260610-003]
Responses: ChatGPT + Grok | 2026-06-10
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

RESPONSES: ChatGPT (CASCADE) | Grok (PARALLEL+early-exit) | Copilot (PARALLEL)
CONSENSUS (3/3 đồng ý):
  ✅ VITAL: SKIP chart production; Copilot: log to meta for research
  ✅ MEDICATION: VALIDATE + SUPPLEMENT (không disable, không primary)
  ✅ APPROVE_WITH_CHANGES

DIVERGENCE resolved:
  ChatGPT: CASCADE
  Grok + Copilot: PARALLEL (2/3 majority)
  → SYNTHESIS: PARALLEL (correct) + OPTIONAL early-exit
    Copilot's key insight: "Rule-based không có confidence → cascade trigger không có cơ sở"
    Optional early-exit: if rule covers trieu_chung + tai_kham + ly_do → skip PhoBERT (CPU opt)
    Default: always run both when use_phobert=True

DECISIONS:

  Q1 ARCHITECTURE: PARALLEL + optional early-exit
     Always run rule-based. When use_phobert=True:
       → Check _has_coverage_gap(entities_rule)
       → IF no gap (all 3 contextual fields filled): optional skip PhoBERT
       → IF gap: PhoBERT runs → merge
     2/3 majority PARALLEL; early-exit addresses ChatGPT's CPU concern.

  Q2 VITAL: SKIP chart (3/3) + LOG to meta (Copilot addition)
     PhoBERT VITAL entity → NOT written to MedicalEntities
     PhoBERT VITAL → written to meta["phobert_vital_detected"] for research

  Q3 MEDICATION: VALIDATE + SUPPLEMENT (3/3)
     L1b INN = primary source of truth
     PhoBERT MEDICATION: confirm (same INN → higher confidence) + supplement (missed → add flagged)
     If PhoBERT detects drug not in L1b AND score ≥ 0.85 → add to don_thuoc flagged_for_review=True

  Q4 CONFIDENCE THRESHOLDS (entity-type-specific, Grok + ChatGPT synthesis):
     MEDICATION, DOSE, FREQUENCY, DURATION: ≥ 0.85 (high safety)
     SYMPTOM, FOLLOWUP, LY_DO:             ≥ 0.75 (recall priority)
     < 0.60: discard (không merge bất kỳ field nào)
     meta["phobert_confidence_avg"] báo cáo cho BS

  Q5 GO CRITERIA (real audio, BENCH-002b, ≥50 transcripts):
     trieu_chung recall ≥ +20% vs rule-only (Copilot 20%; Grok 25% → take 20% conservative)
     F1 (SYMPTOM/FOLLOWUP/MEDICATION) ≥ 0.85 real pilot audio
     False positive rate ≤ 8%
     BS satisfaction ≥ 80% ("triệu chứng đúng, không thêm thuốc sai")
     Latency increase < 600ms vs rule-only
     Critical errors (wrong drug add, missed diagnosis) = 0

NEW RISKS ADDED to FID-VN-009:
  R-009-07: L1b error propagation → PhoBERT sees wrong INN → cascade error risk
  R-009-08: Merge semantic ambiguity — "ho" (rule) vs "ho khan" (PhoBERT) → normalized dedup
  R-009-09: CPU bottleneck — 512MB model → quantize 8-bit if latency > 600ms
  R-009-10: MEDICATION dedup — L1b + PhoBERT overlap → dedup by normalized INN strict
  R-009-11: Symptom over-extraction → filter by diagnosis context
  R-009-12: FOLLOWUP misinterpretation — conditional ("nếu không đỡ") vs definite

## DECISION
[x] APPROVE_WITH_CHANGES → implemented inline in FID-VN-009 (2026-06-10)
[ ] REJECT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## ADDITIONAL REVIEW — ChatGPT Independent Analysis (2026-06-09, post-synthesis)

> **Note:** Review độc lập này được thực hiện SAU synthesis ban đầu. ChatGPT đọc toàn bộ CONS-20260610-003 và đưa ra nhận xét không dựa trên Claude, Copilot, hay Grok trước đó. Một số kết luận KHÁC BIỆT đáng kể so với synthesis — cần xem xét trước khi implement FID-VN-009.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CHATGPT INDEPENDENT REVIEW — CONS-20260610-003
Không dựa trên Claude, Copilot hay Grok
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

EXECUTIVE SUMMARY (ChatGPT)

Nhìn toàn bộ stack MediVoice hiện nay:

  L0  ASR          ← bottleneck lớn nhất
  L1b Drug Fix     ← đang mạnh nhất
  L1c NER          ← CHƯA phải bottleneck
  L2  Form Fill    ← phụ thuộc L0/L1

=> Các cuộc tranh luận về PhoBERT NER hiện tại đang tối ưu hóa
   một tầng CHƯA PHẢI điểm nghẽn chính.

VERDICT: APPROVE_WITH_CHANGES — nhưng vì lý do khác Claude.

─────────────────────────────────────────────────────────────
IMPACT ESTIMATE (ChatGPT ước lượng)

Nếu hôm nay L1c Rule-only:
  Symptom recall   65%
  Followup recall  60%

PhoBERT có thể nâng lên:
  Symptom recall   80-85%
  Followup recall  75-80%
  => gain khoảng 15-20%

Trong khi Drug Recall real-world: 25-40%
  "Ciprofloxacin" → "karaoke tamon" → PhoBERT 99.5% F1 cũng vô dụng

─────────────────────────────────────────────────────────────
Q1. ARCHITECTURE: SHADOW MODE (không chọn Parallel hay Cascade)

  Rule-based → Production output (unchanged)
  PhoBERT    → Logging only (no merge into production)

  Lý do: Hiện tại chưa có:
    - real NER benchmark
    - real PhoBERT F1 (chỉ có synthetic F1=99.44%)
    - real symptom recall data

  F1=99.44% = synthetic → "không tin giá trị đó có liên quan tới production"

  SAU 50-100 transcript thực: compare Rule vs PhoBERT output → log → analyze
  → LÚC ĐÓ mới quyết định Parallel hay Cascade

  [KHÁC synthesis: Synthesis → PARALLEL + early-exit]
  [ChatGPT → SHADOW MODE pilot trước, KHÔNG merge vào production]

─────────────────────────────────────────────────────────────
Q2. VITAL: SKIP hoàn toàn — và mãi mãi (3/3 đồng ý + reinforced)

  Rule-based VITAL = precision ≈ 100% (structured numeric, regex)
  Không có lý do gì dùng transformer cho "130/80"

  ChatGPT đi xa hơn: Disable VITAL training trong PhoBERT hoàn toàn
  → Dùng token budget cho SYMPTOM/FOLLOWUP/DIAGNOSIS (có giá trị hơn)

─────────────────────────────────────────────────────────────
Q3. MEDICATION: VALIDATE ONLY — không supplement

  [KHÁC synthesis: Synthesis → VALIDATE + SUPPLEMENT]
  [ChatGPT → VALIDATE ONLY, KHÔNG supplement]

  Lý do:
    Nếu PhoBERT thấy "Amlodipine" mà L1b không thấy →
    PhoBERT lấy từ đâu? → Từ transcript → Từ ASR
    Nếu ASR đã sai ("ong lau đi pin") → PhoBERT không magically biết đó là Amlodipine
    → Risk FP cực lớn nếu supplement

  => MEDICATION PhoBERT = validate L1b output only
     Nếu L1b miss → NÊN fix L1b/prompt injection, không dùng PhoBERT supplement

─────────────────────────────────────────────────────────────
Q4. CONFIDENCE THRESHOLD: Entity-specific (không hard threshold đồng nhất)

  [Khác synthesis: synthesis có 2-level system 0.85/0.75/0.60]
  [ChatGPT: entity-specific, reflect cost of error]

  Entity       | Threshold | Lý do
  ─────────────────────────────────────────────────────────
  MEDICATION   | 0.90      | Cost sai thuốc >> cost sai triệu chứng
  DIAGNOSIS    | 0.85      |
  FOLLOWUP     | 0.80      |
  SYMPTOM      | 0.75      | Recall priority
  ─────────────────────────────────────────────────────────

  Không thích hard threshold → prefer entity-specific risk-weighted approach

─────────────────────────────────────────────────────────────
Q5. GO CRITERIA: BS Correction Rate (không dùng F1)

  [KHÁC synthesis: Synthesis dùng F1 ≥ 0.85 + trieu_chung recall +20%]
  [ChatGPT: dùng BS Correction Rate — business metric thật]

  Ví dụ:
    100 bệnh án | Rule-only  → BS sửa 40%
    100 bệnh án | Hybrid     → BS sửa 25%
    => GO (BS correction rate drop ≥ 10%)

  Lý do: Metric này phản ánh business value thật
  F1 trên synthetic = không có ý nghĩa production

─────────────────────────────────────────────────────────────
RISKS CẢ 3 AI ĐỀU BỎ SÓT (ChatGPT phát hiện thêm):

  R-NEW-01: SYNTHETIC CONTAMINATION — Possible data leakage
    PhoBERT train trên generate_synthetic_ner.py
    ChatGPT nghi ngờ: train distribution và test distribution quá giống nhau
    F1=99.44% có thể là data leakage dạng nhẹ
    → Cần audit: check train/test overlap, shuffle seed, template diversity

  R-NEW-02: ASR DEPENDENCY TRAP
    Toàn bộ PhoBERT phụ thuộc PhoWhisper output
    WER 50% → NER ceiling bị giới hạn
    "Bạn không thể có ASR 50% + NER 99% trong production"
    → PhoBERT chỉ hữu ích sau khi fix ASR layer trước

  R-NEW-03: WRONG OPTIMIZATION TARGET
    MediVoice đang dành nhiều effort cho NER, ít effort cho decoding
    Data thật (BENCH-002b): ASR + Drug Recall mới là bottleneck
    => "MediVoice hiện đang có ASR problem được ngụy trang thành NER problem"

─────────────────────────────────────────────────────────────
PRIORITY 30 NGÀY TỚI (nếu ChatGPT là CTO dự án):

  Priority 1: GT cho toàn bộ 57 clips
              → KHÔNG phải train model
              → evidence foundation cho mọi quyết định tiếp theo

  Priority 2: Prompt Injection (initial_prompt Whisper)
              → test ngay, zero cost, 2 giờ

  Priority 3: N-best decoding evaluation
              → KHÔNG phải PhoBERT

  Priority 4: Drug pronunciation corpus
              (Ciprofloxacin, Amlodipine, Metformin, Loratadine, Prednisolone...)
              → foundation cho phoneme matching / LoRA data

  Priority 5: LoRA planning
              → CHỈ khi có đủ real audio

─────────────────────────────────────────────────────────────
FINAL VERDICT (ChatGPT):

  Không pivot architecture.
  Giữ: PhoWhisper + L1b Drug Correction + Rule-based NER

  PhoBERT → SHADOW MODE / EVALUATION MODE (chưa phải production)

  Key quote: "MediVoice hiện đang có 'ASR problem được ngụy trang thành
  NER problem'. Dữ liệu BENCH-002b cho thấy phần lớn lỗi nghiêm trọng
  (thuốc, tên riêng, follow-up) xuất hiện từ tầng ASR trước khi PhoBERT
  có cơ hội giúp đỡ. ROI cao nhất 30-60 ngày tới: thu thập GT thực +
  N-best decoding + prompt biasing y khoa + chuẩn bị LoRA data."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## DELTA vs ORIGINAL SYNTHESIS — Điểm khác biệt quan trọng

| Câu hỏi | Synthesis ban đầu (3 AI) | ChatGPT độc lập | Hành động cần làm |
|---|---|---|---|
| Q1 Architecture | PARALLEL + early-exit | **SHADOW MODE** (không merge) | Xem xét lại — shadow mode an toàn hơn |
| Q3 Medication | VALIDATE + **SUPPLEMENT** | **VALIDATE ONLY** | Cần quyết định trước implement |
| Q5 GO Criteria | F1 ≥ 0.85 real audio | **BS Correction Rate drop ≥ 10%** | Thêm business metric |
| Priority | NER hybrid implement | **ASR/GT/Prompt injection trước** | Reorder backlog |
| Risks | R-009-07 đến R-009-12 | **R-NEW-01,02,03** (synthetic leakage, ASR trap) | Add to FID risk register |

**Status:** CONS-20260610-003 cần review lại synthesis trước khi implement FID-VN-009.
Đặc biệt: Q1 (Shadow Mode) và Q3 (Medication supplement OFF) là 2 điểm cần Andy quyết định.

---

## ADDITIONAL REVIEW — Grok Independent Update (2026-06-09, post-synthesis)

> **Note:** Grok đọc lại toàn bộ synthesis + ChatGPT independent review, đưa ra phân tích độc lập cập nhật. Verdict: **APPROVE_WITH_CHANGES** — đủ tốt để implement. Grok tinh chỉnh synthesis, không lật lại quyết định chính.

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
GROK INDEPENDENT UPDATE — CONS-20260610-003
Sau khi đọc ChatGPT, Copilot, Grok ban đầu + synthesis
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: APPROVE_WITH_CHANGES — ĐÃ ĐỦ TỐT ĐỂ IMPLEMENT

Synthesis của Claude khá cân bằng và hợp lý (2/3 majority PARALLEL + early-exit).
Grok có một số tinh chỉnh dựa trên real data failure patterns:
("karaoke tamon", "glibizi glibizi", run-on sentences, central accent)

─────────────────────────────────────────────────────────────
Q1. ARCHITECTURE: PARALLEL + Smart Early-Exit (GIỮ NGUYÊN)

  [Grok giữ PARALLEL — không đồng ý Shadow Mode của ChatGPT]

  Lý do thực tế:
    Rule-based không có confidence score → CASCADE khó trigger chính xác
    Medical domain ưu tiên recall triệu chứng & tái khám
    (miss contextual symptom nguy hiểm hơn latency 200-400ms)
    Real data yếu ở trieu_chung, ly_do, tai_kham → PhoBERT mạnh ở đây

  Smart early-exit: thêm _has_significant_gap(entities_rule)
    → IF rule cover vitals + diagnosis + drugs đầy đủ → optional skip PhoBERT
    → IF gap detected → chạy PhoBERT + merge

  [Response to ChatGPT Shadow Mode: đúng về safety, nhưng mất
  cơ hội thu thập merge conflict data thực tế → không học được gì]

─────────────────────────────────────────────────────────────
Q2. VITAL: SKIP production + LOG meta (3/3 consensus)

  Đồng ý SKIP hoàn toàn cho chart production.
  Bổ sung: log PhoBERT VITAL vào meta["phobert_vital_detected"]
  → dùng để research + improve regex rule-based sau pilot
  → fallback chỉ khi rule-based hoàn toàn miss (edge case)

─────────────────────────────────────────────────────────────
Q3. MEDICATION: VALIDATE + SUPPLEMENT (Grok giữ synthesis)

  [Không đồng ý ChatGPT VALIDATE ONLY]

  L1b = Source of Truth (primary, không thay đổi)
  PhoBERT MEDICATION:
    Confirm: cùng INN → tăng confidence score
    Supplement: L1b miss → add với flagged_for_review=True
    Anomaly: PhoBERT thấy drug KHÁC L1b → flag conflict → BS review

  Lý do giữ supplement:
    Dedup nghiêm ngặt bằng normalized INN + vị trí trong transcript
    Flagged_for_review=True buộc BS confirm thủ công → safe
    Không supplement = bỏ data PhoBERT có thể correct về recall

  Tinh chỉnh dedup: UNION(L1b, PhoBERT) by INN normalized + position window

─────────────────────────────────────────────────────────────
Q4. CONFIDENCE THRESHOLDS (Entity-specific — đồng ý với ChatGPT)

  Entity              | Threshold | Lý do
  ────────────────────────────────────────────────────────────
  MEDICATION/DOSE/    |   0.85    | Safety: cost sai thuốc cao
  FREQUENCY/DURATION  |           |
  SYMPTOM/FOLLOWUP/   |   0.75    | Recall priority (miss = bad UX)
  LY_DO               |           |
  Discard             |   < 0.60  |
  ────────────────────────────────────────────────────────────

  Merge conflict resolution:
    Vitals + Diagnosis: rule-based wins nếu non-empty
    Symptom conflict: ưu tiên cụ thể hơn ("ho khan" > "ho")
    Luôn ghi meta: ai góp field nào + confidence

─────────────────────────────────────────────────────────────
Q5. GO CRITERIA (Real audio, ≥50 transcripts)

  Technical:
    SYMPTOM recall improvement ≥ +20% vs rule-only
    Overall F1 (contextual fields) ≥ 0.85 trên ≥50 real transcripts
    False positive rate (medication) ≤ 8%
    Critical errors (thêm thuốc sai, miss diagnosis) = 0

  UX / Clinical:
    BS satisfaction ≥ 85% (survey sau 20-30 ca pilot)
    Manual correction rate giảm ≥ 25%
    Latency tăng < 550ms trên i5-12400F

  Decision gate: A/B test (rule-only vs hybrid) 40+ real clips
  → Andy + BS review kết quả → GO/NO-GO

─────────────────────────────────────────────────────────────
RISKS bổ sung từ Grok:

  R-009-07: L1b error propagation → PhoBERT thấy transcript sai → hallucination
    Mitigation: Chạy L1b trước, PhoBERT trên transcript đã corrected

  R-009-08: Symptom over-extraction + conditional followup
    Mitigation: Context filter (liên quan diagnosis) +
                distinguish definite vs conditional followup

  R-009-09: CPU/memory production (512MB model)
    Mitigation: Lazy load + 8-bit quantization + early-exit

  R-010 (NEW): MODEL DRIFT trên real accent/noise Đà Nẵng
    Mitigation: Retrain PhoBERT sau mỗi 100-200 corrected transcripts
                (continual learning từ L4 BS corrections)

─────────────────────────────────────────────────────────────
GROK vs CHATGPT — KEY DISAGREEMENTS:

  Q1: Shadow Mode (ChatGPT) vs PARALLEL + early-exit (Grok)
  Grok: Shadow mode safe nhưng miss learning opportunity.
  Better: run both, log all, merge in production với default OFF.
  → Vẫn là safety mode (use_phobert=False default) + collect data

  Q3: VALIDATE ONLY (ChatGPT) vs VALIDATE + SUPPLEMENT (Grok)
  Grok: Supplement với flagged_for_review=True là safe enough.
  BS phải confirm bằng tay → không add drug mà BS không approve.

─────────────────────────────────────────────────────────────
IMPLEMENTATION RECOMMENDATION (Grok):

  Week 1: Implement FID-VN-009 theo synthesis + tinh chỉnh trên
          use_phobert=False default (safe)
          Implement meta logging đầy đủ

  Week 2-4: Chạy shadow mode internal test (Andy team)
            Fill GT cho 57 clips (PA task — prerequisite)
            Benchmark hybrid trên clips có GT

  Month 2: Enable use_phobert=True trên pilot BS Đà Nẵng
           Monitor: correction rate, satisfaction, latency
           Tune threshold nếu cần

  Month 3: GO/NO-GO quyết định dựa trên 50+ real transcripts

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## ADDITIONAL REVIEW — Copilot Independent Update (2026-06-09, post-synthesis)

> **Note:** Copilot đọc toàn bộ CONS-20260610-003 + 4 reviews trước đó, đưa ra phân tích độc lập dựa trên: dữ liệu BENCH-002b, kiến trúc MediVoice, rủi ro SaMD đã loại bỏ, mục tiêu Phase 0 (stability/safety/deterministic), CPU-only constraint. **Key insight mới:** "Nếu bật PhoBERT ngay bây giờ → pipeline sẽ tệ hơn, không tốt hơn."

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COPILOT INDEPENDENT UPDATE — CONS-20260610-003
Dựa trên: BENCH-002b real data + Phase 0 safety constraints
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VERDICT: APPROVE_WITH_CHANGES
→ nhưng chỉ bật PhoBERT SAU KHI:
  ① A1 (drug prompt injection) done
  ② A2 (VAD chunking) done
  ③ A3 (abbreviation expansion) done
  ④ BENCH-002b ≥50 clips real-world completed

"Nếu bật PhoBERT ngay bây giờ → pipeline sẽ tệ hơn, không tốt hơn."

─────────────────────────────────────────────────────────────
Q1. ARCHITECTURE: PARALLEL + EARLY-EXIT (đúng nhất)

  [Copilot: KHÔNG chọn CASCADE, KHÔNG chọn Shadow Mode]

  Vì sao CASCADE sai:
    Rule-based không có confidence score
    Không có coverage score / uncertainty estimate / calibration
    → CASCADE không có trigger hợp lý
    → Sẽ miss triệu chứng, follow-up, lý do khám

  Vì sao PARALLEL đúng:
    Medical domain = high-stakes → ưu tiên recall
    Rule-based mạnh: VITAL + DIAGNOSIS
    PhoBERT mạnh: SYMPTOM + FOLLOWUP + LY_DO
    Hai nguồn bổ sung nhau, không thay thế
    Latency CPU 300-500ms chấp nhận được

  EARLY-EXIT (tối ưu):
    IF rule-based fill đủ trieu_chung + tai_kham + ly_do
    → skip PhoBERT (tiết kiệm CPU)
    Đây là kiến trúc tối ưu nhất cho Phase 0

─────────────────────────────────────────────────────────────
Q2. VITAL: SKIP hoàn toàn + LOG meta (đồng ý)

  Regex VITAL cực mạnh (nhiet_do, huyet_ap, mach, spo2)
  PhoBERT synthetic → dễ hallucinate số → sai VITAL = sai lâm sàng nghiêm trọng
  → LOG PhoBERT VITAL vào meta để phân tích (không dùng vào chart)

─────────────────────────────────────────────────────────────
Q3. MEDICATION: VALIDATE + SUPPLEMENT (đúng nhất)

  [Copilot: KHÔNG disable, KHÔNG primary, KHÔNG validate-only]

  L1b = primary source of truth (INN normalized)
  PhoBERT MEDICATION:
    Confirm: cùng INN → tăng confidence
    Supplement: L1b miss → add flagged_for_review=True (BS phải confirm tay)
    Conflict: L1b thắng → PhoBERT flagged

  Merge logic:
    L1b INN = source of truth
    UNION(L1b, PhoBERT) by normalized INN + position window
    Conflict resolution: L1b wins, PhoBERT conflict → flag

  "Đây là cách duy nhất tránh critical error (thêm thuốc sai)"

─────────────────────────────────────────────────────────────
Q4. CONFIDENCE THRESHOLDS (Entity-specific + flagged zone)

  Entity                       | Accept   | Flagged zone  | Discard
  ──────────────────────────────────────────────────────────────────
  MEDICATION/DOSE/FREQ/DURATION| ≥ 0.85   | 0.60–0.85     | < 0.60
  SYMPTOM/FOLLOWUP/LY_DO       | ≥ 0.75   | 0.50–0.75     | < 0.50
  ──────────────────────────────────────────────────────────────────

  Lý do entity-specific:
    Thuốc = high-risk → threshold cao
    Triệu chứng = high-recall → threshold thấp hơn
    Follow-up = medium-risk → threshold trung bình

─────────────────────────────────────────────────────────────
Q5. GO CRITERIA (Real-world, ≥50 transcripts BENCH-002b)

  GO nếu:
    trieu_chung recall ≥ +20% vs rule-only
    Medication recall ≥ 95% (sau L1b + PhoBERT)
    False positive rate ≤ 8%
    Critical errors = 0 (thêm thuốc BS không nói / sai liều / sai follow-up)
    BS satisfaction ≥ 80%
    Latency tăng < 600ms
    F1 real-world ≥ 0.85 (SYMPTOM/FOLLOWUP/MEDICATION)

  NO-GO nếu:
    PhoBERT thêm thuốc sai
    PhoBERT thêm follow-up sai
    PhoBERT hallucinate triệu chứng
    BS không tin output

─────────────────────────────────────────────────────────────
RISKS BỔ SUNG (3 risks mới, chưa có trong synthesis):

  R-009-13: ACCENT MISMATCH
    PhoBERT train synthetic → không hiểu giọng Đà Nẵng / Cần Thơ
    Mitigation: Retrain sau khi có real transcripts từ pilot

  R-009-14: CONTEXT HALLUCINATION
    PhoBERT có thể tạo triệu chứng không có trong transcript nếu synthetic bias
    Mitigation: Confidence threshold + BS mandatory review cho PhoBERT fields

  R-009-15: MERGE EXPLOSION
    PhoBERT over-extract → chart có 10 triệu chứng → BS reject
    Mitigation: Cap trieu_chung ≤ 5 items; relevance filter vs chan_doan

─────────────────────────────────────────────────────────────
TÓM TẮT 1 CÂU (Copilot):

  "FID-VN-009 đúng hướng, nhưng chỉ nên bật PhoBERT sau khi ASR được sửa
  (A1/A2/A3) và BENCH-002b real-world đạt tiêu chí GO.
  Kiến trúc đúng nhất: PARALLEL + EARLY-EXIT + MEDICATION VALIDATE+SUPPLEMENT
  + VITAL SKIP."

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## FINAL DECISION TABLE — Tổng hợp 6 nguồn (cập nhật)

| Câu hỏi | ChatGPT #1 | Copilot #1 | Grok #1 | ChatGPT Indep. | Grok Update | Copilot Update | Consensus |
|---|---|---|---|---|---|---|---|
| Q1 Architecture | CASCADE | PARALLEL | PARALLEL+exit | **SHADOW MODE** | PARALLEL+exit | **PARALLEL+exit** | **PARALLEL+exit** (4/5) |
| Q2 Vital | SKIP | SKIP+log | SKIP+fallback | SKIP forever | SKIP+log | SKIP+log | **SKIP+log meta** (5/5) |
| Q3 Medication | VALIDATE+SUPP | VALIDATE+SUPP | VALIDATE+SUPP | **VALIDATE ONLY** | VALIDATE+SUPP | **VALIDATE+SUPP** | **VALIDATE+SUPP** (5/6) |
| Q4 Threshold | 2-level | simple 0.80 | entity-specific | entity-specific | entity-specific | entity-specific + flagged zone | **Entity-specific** (4/5) |
| Q5 GO Criteria | F1+correction | F1 real | F1+latency+sat | **BS Correction Rate** | F1+sat+latency | F1+BS sat+critical=0 | **F1 real + BS Correction Rate + Critical=0** |
| Prerequisite | — | — | — | ASR first | — | **A1+A2+A3 first** | **ASR fixes TRƯỚC khi bật** |

**Risks tổng hợp từ tất cả nguồn:**
```
R-009-07: L1b error propagation → PhoBERT sees wrong INN
R-009-08: Merge semantic ambiguity ("ho" vs "ho khan")
R-009-09: CPU bottleneck 512MB
R-009-10: MEDICATION dedup strict
R-009-11: Symptom over-extraction
R-009-12: FOLLOWUP definite vs conditional
R-NEW-01: Synthetic contamination (ChatGPT)
R-NEW-02: ASR dependency trap (ChatGPT)
R-NEW-03: Wrong optimization target (ChatGPT)
R-010:    Model drift trên real accent (Grok)
R-009-13: Accent mismatch (Copilot)
R-009-14: Context hallucination (Copilot)
R-009-15: Merge explosion >5 triệu chứng (Copilot)
```

**Andy quyết định cuối cùng (2 điểm còn mở):**

- Q1: **PARALLEL+early-exit (4/5)** vs Shadow Mode (ChatGPT Indep 1/5) — Copilot và Grok update đều PARALLEL
- Q3: **VALIDATE+SUPPLEMENT (5/6)** vs VALIDATE ONLY (ChatGPT Indep 1/6) — consensus rất rõ

**Prerequisite mới (Copilot insight):** Bật PhoBERT chỉ sau khi A1+A2+A3 done + BENCH-002b ≥50 clips.

---

*CONS-20260610-003 | FID-VN-009 Review — Hybrid NER*
*Status: CLOSED — Andy chốt 2026-06-09: Q1=PARALLEL+early-exit · Q3=VALIDATE+SUPPLEMENT*
*Reviews: ChatGPT × 2 + Copilot × 2 + Grok × 2 = 6 perspectives*
