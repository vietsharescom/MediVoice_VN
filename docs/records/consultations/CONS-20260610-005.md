# CONS-20260610-005 — CT-014 / FID-VN-013 Voice Calibration scope

> Copy phần trong khối ``` bên dưới, paste vào ChatGPT/Grok/Gemini/Copilot.
> Sau khi có response, paste lại cho Claude → Claude sẽ synthesize vào CONSULTATION SYNTHESIS.

---

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSULTATION REQUEST [CONS-20260610-005]
From: Claude Sonnet 4.6 | MediVoice VN | SES-20260610
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PROJECT CONTEXT
Product: MediVoice VN — AI voice scribe cho phòng mạch tư VN
  BS nói → PhoWhisper ASR → NER → Mẫu 15/BV-01 → BS approve → PDF
Stack: Python 3.10 / FastAPI / PhoWhisper-medium / PhoBERT+CRF / SQLite / Fernet / PWA
Constraints:
  - 100% offline (NĐ13/2023: data tại VN, không cloud nước ngoài)
  - NOT SaMD (chỉ transcription + form fill, không tự chẩn đoán)
  - BS phải approve mọi record (Luật KCB 2023 Điều 62 — L4 Human Gate, KHÔNG bypass)
  - Output: Mẫu 15/BV-01 tiếng Việt (TT32/2023)
  - Pipeline L0→L10 FROZEN — thay đổi chỉ qua FID
Phase: Phase 0 pilot — 5 BS phòng mạch tư Đà Nẵng + Sài Gòn
ISO: ISO/IEC 42001:2023 + ISO 9001:2015

## QUESTION
Andy (owner) muốn mở 1 tính năng mới "FID-VN-013 — Voice Calibration / DVP Layer 3":
khi BS đăng ký (optional, có thể bỏ qua), AI "chẩn đoán tông giọng" của BS (tốc độ nói,
ngắt nghỉ, vùng miền) qua waveform UI "AI đang nghe", và auto-adapt theo giọng/tốc độ BS
trong lúc dùng. Cần xác định scope cho FID-VN-013 v1.

## OPTIONS EVALUATED

### Option A: Waveform UI + expose signals đã có (frontend-only)
Mô tả: Thêm waveform visualization (Web Audio API, canvas) hiển thị "AI đang nghe" trong
lúc ghi âm. Hiển thị real-time: vùng miền phát hiện được (đã có `dialect_norm.detect_region()`
— A3, DONE) + điểm ngắt câu (đã có `silero-vad` pause detection — A2-VAD, DONE nhưng
CHƯA wire vào /transcribe vì CT-019 regression). KHÔNG train/finetune model nào, KHÔNG
lưu thêm dữ liệu giọng nói.
Pros:
  - Risk thấp, không đụng pipeline L0-L10 (chỉ frontend + expose field có sẵn)
  - Effort thấp (~1-2 ngày)
  - Tăng trust UX cho BS lần đầu dùng AI (BS VN chưa quen AI voice)
Cons:
  - KHÔNG cải thiện accuracy ASR thực sự — chỉ là UX/trust layer
  - "Auto-adapt theo tốc độ nói" Andy mô tả sẽ chỉ là hiển thị, chưa thực sự đổi behavior model
Risks: Thấp — không vi phạm constraint nào
Effort: Low | Timeline: 1-2 ngày

### Option B: + Per-speaker LoRA adapter (model personalization)
Mô tả: Thêm vào Option A — lúc đăng ký, ghi 1-2 phút giọng BS, fine-tune 1 LoRA adapter
nhỏ (vài MB) cho PhoWhisper, lưu theo `doctor_id` (đã có DoctorProfile schema từ
FID-VN-012/DVP). Theo nghiên cứu 2025-2026 (LoRA speaker adaptation trên Whisper),
giảm WER 15-24% cho giọng BS đó.
Pros:
  - Cải thiện accuracy thực sự cho từng BS (không chỉ UX)
  - Tận dụng infra DoctorProfile đã có (FID-VN-012)
  - Train trên CPU vài phút (không cần GPU cloud như TRAIN-001)
Cons:
  - Cần infra fine-tuning mới (chưa có trong pipeline hiện tại)
  - Cần audio enrollment 1-2 phút/BS — có cần consent riêng không?
  - Tăng độ phức tạp maintain (1 adapter / BS, version drift theo PhoWhisper base model)
Risks: Privacy — lưu LoRA adapter weights theo doctor_id; cần xác nhận có coi là "dữ liệu
sinh trắc học giọng nói" theo NĐ13/2023 không, dù KHÔNG lưu raw audio
Effort: Medium | Timeline: 1-2 tuần (bao gồm infra fine-tuning + tests)

## HARD CONSTRAINTS (KHÔNG thể vi phạm)
- KHÔNG lưu raw audio sau xử lý (Privacy by Design, L0 `purge_audio()`) — nếu Option B
  cần audio enrollment, phải xử lý xong và xóa raw audio ngay sau khi train adapter
- TRAIN-001 (fine-tune PhoWhisper full model trên 50-100h audio pilot) đã được Andy
  xác định là ƯU TIÊN CAO NHẤT (quyết định CT-028, 2026-06-10) — hiện đang BLOCKED vì
  cần GPU/cloud VM + audio pilot thật. FID-VN-013 KHÔNG được làm chậm trễ TRAIN-001.
- L4 Human Gate không được bypass dù có personalization

## CLAUDE'S CURRENT ANALYSIS
Lean toward: Option A (v1 scope = A only, B deferred sang Phase 1 sau TRAIN-001)
Confidence: 75%
Main reason: A2-VAD + A3-dialect đã DONE sẵn, chỉ cần "wiring" + UI — value/risk ratio
  cao nhất, không cạnh tranh resource với TRAIN-001.
Main uncertainty: (1) Liệu Option B (LoRA) có nên làm SONG SONG với TRAIN-001 (khác
  loại tài nguyên — CPU vs GPU, có thể không conflict) thay vì "sau"? (2) Câu hỏi
  privacy về LoRA adapter weights — cần ý kiến độc lập về NĐ13/2023 áp dụng cho
  "voice fingerprint" dạng model weights.

## WHAT I NEED FROM YOU
Please provide:
1. Your recommendation: A / B (A+B song song) / other — with reasoning
2. Confidence level: X%
3. Key risks or considerations I may have missed
4. Is this an owner (Andy) decision vs technical decision? Why?
5. If owner decision: what specific question should Andy answer?
6. Đặc biệt: ý kiến về câu hỏi privacy (LoRA adapter weights theo doctor_id, không lưu
   raw audio — có nên coi là "dữ liệu sinh trắc học" cần consent riêng theo NĐ13/2023
   không, theo hiểu biết chung về data privacy law tương tự GDPR biometric data)?

Return format:
  RECOMMENDATION: [A/B/other]
  CONFIDENCE: [X%]
  KEY RISKS: [list]
  OWNER DECISION: [yes/no + why]
  OWNER QUESTION: [if yes, what to ask]
  PRIVACY OPINION: [LoRA adapter weights = biometric data? yes/no + reasoning]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## RESPONSE ĐÃ NHẬN (Groq/LLaMA-3.3-70B, qua orchestrator — 1/4 providers hoạt động)

> Lưu ý: 3/4 providers (OpenAI/xAI/OpenRouter) hiện không hoạt động (quota/credits —
> xem `docs/records/consultations/ORCH-CONSULT-20260610-223659.json`). Response dưới
> đây CHƯA đủ để làm "consensus" — Andy paste thêm response từ ChatGPT/Grok/Gemini
> manual để Claude synthesize đầy đủ.

```
RECOMMENDATION: A (v1 scope = A only)
CONFIDENCE: ~70% (ước tính từ câu trả lời, Groq không nêu rõ %)
KEY RISKS: (không nêu rõ ngoài câu 3 dưới)
OWNER DECISION: (không trả lời rõ — câu trả lời tập trung vào kỹ thuật)
PRIVACY OPINION: LoRA weights không lưu raw audio → có thể KHÔNG vi phạm Privacy by
  Design / NĐ13, nhưng cần chuyên gia bảo mật xác nhận trước khi triển khai.
```

---

## RESPONSE 2 — Copilot (2026-06-10)

```
RECOMMENDATION: A (v1 = Option A only)
CONFIDENCE: 85%
KEY RISKS:
  - Option B ngay bây giờ: regulatory risk (LoRA adapter = biometric template),
    operational risk (1 adapter/BS, drift theo PhoWhisper base version),
    strategic risk (delay TRAIN-001), UX risk (enrollment friction), legal risk (cần DPIA)
  - Option A: không cải thiện accuracy thật — nhưng đây là v1, mục tiêu trust+adoption
OWNER DECISION: YES — vì liên quan pháp lý (biometric data), product strategy
  (personalization vs universal model), UX (enrollment friction), resource allocation
  (delay TRAIN-001 hay không)
OWNER QUESTION: "MediVoice VN có muốn trở thành sản phẩm personalized per-doctor
  (giọng, tốc độ, accent), hay muốn giữ universal model để dễ maintain/scale?"
  - Nếu "universal" → Option A v1, Option B → Phase 1
  - Nếu "personalized" → phải chấp nhận consent riêng + DPIA + maintenance overhead +
    delay TRAIN-001
PRIVACY OPINION: 70% khả năng LoRA adapter weights = "dữ liệu sinh trắc học" theo
  NĐ13/2023 (voiceprint/biometric template — mã hoá đặc trưng giọng nói cá nhân, dùng
  để phân biệt BS A vs BS B dù không lưu raw audio). Hệ quả: cần consent riêng (không
  gộp vào consent chung), DPIA, cơ chế xoá adapter theo yêu cầu, policy cấm dùng adapter
  để nhận dạng danh tính.
```

---

## RESPONSE 3 — ChatGPT (2026-06-10)

```
RECOMMENDATION: A (v1) + thiết kế sẵn kiến trúc cho B, nhưng KHÔNG triển khai B trước
  TRAIN-001
CONFIDENCE: 88%
KEY RISKS:
  - Sai thứ tự ưu tiên: bottleneck thật của MediVoice là (a) thiếu audio pilot thật,
    (b) Drug Error Rate cao, (c) chưa có TRAIN-001 results — KHÔNG phải UI/personalization
  - LoRA có thể CHE GIẤU vấn đề base model: 1 BS có WER thấp nhờ adapter riêng → đánh
    giá quá cao chất lượng hệ thống → khi scale sang BS thứ 6/20/100 vẫn gặp lại vấn đề
    base model
  - Technical debt model lifecycle: mỗi lần TRAIN-001 ra PhoWhisper v2 → tất cả LoRA
    adapter cũ (A, B, C...) có thể phải retrain + version + migrate + benchmark lại —
    chi phí vận hành tăng theo SỐ BÁC SĨ
  - Privacy: KHÔNG nên giả định "không lưu audio = hết rủi ro" — LoRA adapter ở "vùng
    xám" giữa raw audio (chắc chắn personal data) và speaker embedding (gần như chắc
    chắn biometric)
  - Scope creep: Option B thực chất = enrollment + adaptation + adapter lifecycle +
    privacy + consent management = 1 dự án riêng, không còn là "voice calibration"

RECOMMENDED V1 SCOPE (chi tiết hơn 2 response trước):
  - Waveform realtime
  - Microphone level indicator
  - Dialect detection (Bắc/Trung/Nam)
  - Speaking speed indicator
  - Pause detection
  - Recording quality score
  - Gợi ý hành vi: "Nói chậm hơn một chút" / "Micro quá xa" / "Môi trường có tiếng ồn"
  - Constraint: KHÔNG đổi ASR model, KHÔNG lưu thêm dữ liệu, KHÔNG tạo thêm regulatory
    burden

OPTION B → "Phase 1 Research Track", điều kiện mở:
  - TRAIN-001 hoàn tất
  - Có ≥50-100h audio thật
  - Có baseline WER theo từng BS
  - Chứng minh được speaker variance là nguồn lỗi LỚN (mới đáng đầu tư LoRA)

OWNER DECISION: YES — đây là quyết định CHIẾN LƯỢC SẢN PHẨM:
  A. Sản phẩm dễ triển khai, ít dữ liệu cá nhân, ít vận hành
  B. Sản phẩm cá nhân hoá sâu theo từng BS, đổi lại quản lý thêm consent/adapter/
     lifecycle/privacy

OWNER QUESTION: "Nếu TRAIN-001 giúp giảm WER xuống mức chấp nhận được cho ĐA SỐ bác sĩ,
  tôi có còn muốn đầu tư personalization theo từng BS nữa không?"
  - KHÔNG → LoRA không nên tồn tại
  - CÓ → chấp nhận chi phí vận hành + compliance tăng

PRIVACY OPINION: KHÔNG mặc định LoRA adapter = "không phải biometric data".
  - Raw audio = chắc chắn personal data
  - Speaker embedding = gần như chắc chắn biometric data
  - LoRA adapter = vùng xám — nếu adapter riêng cho 1 BS và CÓ KHẢ NĂNG giúp nhận diện/
    tái tạo đặc trưng giọng → giả định LÀ biometric data
  Khuyến nghị nếu làm Option B: consent riêng + mô tả rõ mục đích personalization +
  cho phép BS xoá adapter + xoá audio enrollment ngay sau train + tách adapter khỏi
  dữ liệu bệnh nhân
```

---

## RESPONSE 4 (2026-06-10)

```
RECOMMENDATION: A (v1 scope = A only, B defer sang Phase 1 sau TRAIN-001)
CONFIDENCE: 82%
KEY RISKS:
  Option A: Rủi ro thấp nhất. Chỉ là UX layer, không ảnh hưởng pipeline core. Main risk
    là BS kỳ vọng quá cao ("AI adapt theo giọng tôi") nhưng thực tế chỉ visualize → cần
    UI/tooltip giải thích rõ ràng.
  Option B:
    - Resource conflict nhẹ với TRAIN-001 (dù CPU vs GPU)
    - Maintainability: Version drift của LoRA adapters khi update base PhoWhisper
    - Privacy & Compliance: Nguy cơ bị coi là xử lý dữ liệu sinh trắc học (voice
      fingerprint qua model weights)
    - Enrollment experience: BS bận rộn, có thể skip hoặc làm qua loa → chất lượng
      adapter kém

  Missed consideration: Impact trên PWA offline (LoRA load per doctor_id có thể tăng
    thời gian init session)

OWNER DECISION: Yes (phần lớn)
OWNER QUESTION:
  "Anh muốn ưu tiên gì cho Phase 0 pilot: (1) Tăng trust & UX nhanh nhất có thể với
  effort thấp (Option A), hay (2) Chấp nhận effort trung bình + rủi ro privacy/
  compliance để có accuracy cải thiện rõ rệt ngay từ pilot (Option B song song)?"
  Cũng hỏi thêm: Anh chấp nhận BS phải ghi enrollment 1-2 phút giọng nói không, và có
  muốn consent form riêng cho voice profile không?

PRIVACY OPINION:
  Yes, LoRA adapter weights nên được coi là dữ liệu sinh trắc học gián tiếp
  (sensitive/biometric-derived data).
  Lý do:
    - Theo NĐ13/2023/NĐ-CP (và Luật Bảo vệ dữ liệu cá nhân 91/2025/QH15), dữ liệu sinh
      trắc học bao gồm đặc điểm giọng nói (voice characteristics) — đây là dữ liệu
      nhạy cảm, cần consent rõ ràng, đánh giá tác động (DPIA), và biện pháp bảo vệ
      cao hơn.
    - Mặc dù không lưu raw audio (tuân thủ purge_audio()), nhưng LoRA weights là "voice
      fingerprint" — nó encode đặc trưng tông giọng, tốc độ, accent của BS cụ thể → vẫn
      có thể reconstruct một phần identity hoặc dùng để xác thực. Tương tự biometric
      templates dưới GDPR (không phải raw nhưng vẫn sensitive).
    - Khuyến nghị: Xử lý như sensitive data → cần consent riêng, mã hóa mạnh (đã có
      Fernet), access control theo doctor_id, và audit log. Nên hỏi ý kiến luật sư/
      data protection officer trước khi triển khai Option B.

Tóm lại: đồng ý bắt đầu bằng Option A để pilot suôn sẻ, build trust, wiring những thứ
  đã có (waveform + dialect + VAD). Sau khi TRAIN-001 xong và có dữ liệu pilot thật,
  mới cân nhắc LoRA (sẽ hiệu quả hơn nhiều).
```

---

## CONSULTATION SYNTHESIS (5-AI)

| AI | Recommendation | Confidence | Key Points |
|----|---------------|------------|------------|
| Claude (original) | Option A | 75% | A2-VAD+A3-dialect đã có, value/risk cao nhất, không cạnh tranh resource với TRAIN-001 |
| Groq/LLaMA-3.3-70B | Option A | ~70% (ước tính) | TRAIN-001 ưu tiên trước; LoRA privacy "có thể OK" nhưng cần security expert |
| Copilot | Option A | 85% | Option B = biometric template risk (70%) + delay TRAIN-001 + maintenance overhead |
| ChatGPT | Option A (+ design B, không implement) | 88% | Bottleneck thật = audio pilot+TRAIN-001, không phải UI; LoRA che giấu vấn đề base model; technical debt lifecycle theo SỐ BÁC SĨ; privacy = "vùng xám", giả định là biometric |
| AI #4 | Option A (B defer Phase 1) | 82% | Mới: rủi ro kỳ vọng quá cao của BS ("AI adapt theo giọng tôi" — chỉ là visualize) → cần tooltip rõ ràng; mới: PWA offline init-time impact nếu load LoRA per doctor_id; trích dẫn thêm Luật Bảo vệ dữ liệu cá nhân 91/2025/QH15 |

### AGREEMENT ANALYSIS
Points ALL 5 AI agree on:
  - **FID-VN-013 v1 = Option A only** — waveform UI + behavioral feedback, KHÔNG đổi
    ASR model, KHÔNG lưu thêm dữ liệu, không đụng pipeline FROZEN
  - **Option B (per-speaker LoRA) phải đợi sau TRAIN-001** — và theo ChatGPT, cần thêm
    điều kiện: có baseline WER per-BS + chứng minh speaker variance là nguồn lỗi lớn
    (không chỉ "đợi xong TRAIN-001" mà còn phải "TRAIN-001 cho thấy LoRA còn cần thiết")
  - **Privacy của LoRA adapter weights = rủi ro thật, nên giả định LÀ biometric data**
    cho đến khi có luật sư VN xác nhận khác (4/5 AI nghiêng rõ về hướng này — AI #4
    còn trích thêm Luật Bảo vệ dữ liệu cá nhân 91/2025/QH15; Groq trung lập)
  - **Option A cần UX/tooltip giải thích rõ "đây là visualization, KHÔNG phải AI tự
    động đổi behavior theo giọng BS"** (AI #4) — tránh kỳ vọng sai của BS, nhất quán
    với Absolute Rule #8 (UI hiển thị "AI tạo nháp — BS chịu trách nhiệm")

Points with DISAGREEMENT / BỔ SUNG MỚI:
  - Issue: Phạm vi cụ thể của Option A v1
    Claude/Groq/Copilot: chỉ nói chung "waveform UI + expose VAD/dialect signals"
    ChatGPT: liệt kê CỤ THỂ 7 hạng mục — waveform, mic level indicator, dialect
    detection, speaking speed indicator, pause detection, **recording quality score**,
    và **behavioral hints** ("Nói chậm hơn", "Micro quá xa", "Môi trường ồn")
    Resolution: dùng list của ChatGPT làm baseline scope cho FID-VN-013 v1 — cụ thể
    hơn, actionable hơn, và mic-level/recording-quality-score là điểm MỚI chưa AI nào
    khác đề cập (giá trị thực: giảm lỗi do thu âm kém — vốn là 1 nguyên nhân WER cao
    theo BENCH-002b)

  - Issue: Option B nên "defer" hay "design sẵn kiến trúc nhưng không implement"
    Claude/Groq/Copilot: defer hoàn toàn, không thiết kế trước
    ChatGPT: thiết kế sẵn kiến trúc cho B (vd: DoctorProfile schema đã sẵn sàng nhận
    thêm field `lora_adapter_path`) nhưng KHÔNG implement, để tránh rework sau
    Resolution: ChatGPT's "design for B, build A" hợp lý — không tốn effort thêm vì
    DoctorProfile (FID-VN-012) đã có `doctor_id`, chỉ cần ghi chú trong FID-VN-013 rằng
    schema này CÓ THỂ mở rộng cho Phase 1, không cần làm gì thêm bây giờ

  - Issue: Gating condition cho Option B
    Claude/Groq/Copilot: "sau TRAIN-001"
    ChatGPT: thêm 2 điều kiện cụ thể — (1) có baseline WER per-BS, (2) chứng minh
    speaker variance là nguồn lỗi LỚN (không phải chỉ "có thời gian rảnh")
    Resolution: dùng gating conditions của ChatGPT — chặt chẽ hơn, tránh việc làm
    Option B "vì có thể làm" mà không có evidence nó giải quyết đúng vấn đề

  - Issue: "B sau TRAIN-001" hay "B song song với TRAIN-001 ngay từ pilot"
    AI #4: nêu Option "B song song" như 1 lựa chọn khả dĩ cho owner question (effort
    trung bình, accuracy cải thiện ngay từ pilot), nhưng KẾT LUẬN cuối vẫn nghiêng A
    only trước, B sau khi có TRAIN-001 + dữ liệu pilot thật
    4/5 AI khác: B chỉ nên xét SAU TRAIN-001, không song song (tránh resource/scope
    conflict + che giấu vấn đề base model — ChatGPT)
    Resolution: GIỮ "B sau TRAIN-001" làm khuyến nghị chính — AI #4 cũng đồng ý ở phần
    kết luận, "song song" chỉ là 1 option liệt kê trong owner question chứ không phải
    final recommendation của chính AI #4

## FINAL RECOMMENDATION
Recommendation: **Option A** cho FID-VN-013 v1, scope theo list 7 hạng mục của ChatGPT
  + thêm 2 lưu ý UX/kỹ thuật từ AI #4 (xem dưới)
Consolidated confidence: 85% (5/5 AI đồng ý A cho v1, dao động 70-88%)

Because:
  1. 5/5 AI đồng thuận Option A cho v1 — risk thấp, tận dụng A2-VAD + A3-dialect đã DONE,
     không đụng pipeline L0-L10 FROZEN, KHÔNG tạo regulatory burden mới
  2. TRAIN-001 (CT-028, ưu tiên #1 của Andy) không bị delay — Option A không cạnh tranh
     resource, và theo ChatGPT, làm Option B SỚM còn có nguy cơ "tối ưu cục bộ" che
     giấu vấn đề base model thật
  3. Option B (LoRA personalization) có rủi ro pháp lý CHƯA RESOLVED + technical debt
     lifecycle tăng theo số BS — gating rõ ràng: TRAIN-001 xong + baseline WER per-BS +
     chứng minh speaker variance là nguồn lỗi lớn

Bổ sung từ AI #4 vào scope FID-VN-013 v1:
  - UI/tooltip giải thích rõ waveform/dialect/pause display CHỈ LÀ VISUALIZATION, AI
    KHÔNG tự động đổi cách xử lý theo giọng BS (tránh kỳ vọng sai — nhất quán Rule #8)
  - Lưu ý kỹ thuật: nếu sau này làm Option B, cần đo PWA offline init-time impact khi
    load LoRA adapter theo doctor_id (chưa phải vấn đề cho v1 vì v1 không có adapter)

Risk if wrong: LOW — Option A là incremental UI/UX improvement, dễ revert

## OWNER DECISION NEEDED?
YES

Decision for Andy (câu hỏi tổng hợp từ Copilot + ChatGPT, đã refine):

**"Sau khi TRAIN-001 hoàn tất và giảm WER xuống mức chấp nhận được cho ĐA SỐ bác sĩ,
Andy có còn muốn đầu tư vào personalization theo từng BS (Option B — per-speaker LoRA,
kèm consent riêng + DPIA + adapter lifecycle management) hay không? Hay MediVoice nên
giữ triết lý 'universal model — 1 PhoWhisper chung, dễ vận hành, ít dữ liệu cá nhân'?"**

Pro Option A only (universal model):
  - Không vướng biometric data / NĐ13/2023 — không cần consent riêng, không cần DPIA
  - Maintenance đơn giản — 1 model cho tất cả BS, đồng bộ với TRAIN-001 roadmap
  - Phù hợp pilot scale nhỏ (5-10 BS) — chưa cần personalization
  - Tránh "tối ưu cục bộ che giấu vấn đề base model" (ChatGPT)

Pro Option A+B (personalized, sau TRAIN-001):
  - Cải thiện WER thực sự cho từng BS (15-24% theo nghiên cứu) — có thể giúp BS có
    giọng khó (HN, miền Trung) đạt parity với BS giọng dễ (SG)
  - Tận dụng DoctorProfile (FID-VN-012) đã có sẵn `doctor_id` — chỉ cần thêm field

RECOMMENDATION: **Option A only cho v1, KHÔNG quyết định B ngay** — câu hỏi owner
  decision này chỉ cần trả lời SAU KHI có kết quả TRAIN-001 (không block FID-VN-013 v1)
UNLESS: Andy muốn quyết định triết lý sản phẩm SỚM để định hướng roadmap dài hạn
  (vd: nếu chắc chắn "KHÔNG bao giờ làm personalization" → có thể đơn giản hoá
  DoctorProfile schema, bỏ field doctor_id riêng cho mục đích model adaptation)
DEADLINE: Không gấp cho v1. Câu hỏi đầy đủ cần trả lời TRƯỚC khi TRAIN-001 hoàn tất
  (để kịp lên kế hoạch Phase 1 nếu chọn "CÓ")

IF Andy chọn "Option A only, quyết B sau":
  Claude viết FID-VN-013 scope = 7 hạng mục theo ChatGPT (waveform, mic level, dialect
  detection, speaking speed, pause detection, recording quality score, behavioral
  hints), note kiến trúc DoctorProfile có thể mở rộng cho Phase 1 (không cần làm gì
  thêm bây giờ), Option B ghi vào BACKLOG Phase 1 với gating conditions của ChatGPT
  (TRAIN-001 done + baseline WER per-BS + speaker variance proven significant).

---

## ADDENDUM (2026-06-10) — Andy follow-up + Grok review của scope v2

Sau synthesis 5-AI ở trên, Andy bổ sung 2 yêu cầu kỹ thuật cụ thể chưa được Option A
cover:
1. Hệ thống phân tích pitch/tần số giọng BS → tự điều chỉnh layer xử lý sau
2. BS đọc tên thuốc tiếng Anh theo cách riêng (vd "Amlodipine" → "âm lô phin" vs
   "am lo di phin") → hệ thống học + ghi lại mapping

Claude phân tích: đây là 2 vấn đề tách biệt, có thể giải bằng giải pháp risk THẤP
(không phải LoRA):
- (1) → **VTLN (Vocal Tract Length Normalization)** — signal processing cổ điển,
  output = 1 scalar/BS, áp tại L0 trước PhoWhisper
- (2) → **Drug Pronunciation Enrollment Wizard** — text alias dictionary, dùng lại
  schema `doctor_aliases` (FID-VN-012), BS đọc 15-20 thuốc lúc enrollment, hệ thống
  so transcript với ground truth → propose alias → BS confirm

### Grok review (2026-06-10)

```
RECOMMENDATION: Mở rộng FID-VN-013 để bao gồm Option A + Drug Alias Wizard + VTLN
  (tách LoRA ra sau)
CONFIDENCE: 85%
KEY RISKS:
  - Drug Wizard: BS có thể đọc không chuẩn (mệt, accent mạnh) → cần fallback "manual
    edit alias" dễ dùng
  - VTLN: Cần test compatibility với PhoWhisper (torchaudio/librosa cho warping). Nên
    thử trên dataset pilot nhỏ trước
  - Scope creep: đừng nhét quá nhiều vào 1 FID — nếu cần, tách Drug Wizard thành
    FID-VN-014
  - Offline constraint: cả 2 tính năng mới đều 100% offline được

OWNER DECISION: YES
OWNER QUESTION: "Anh muốn Phase 0 pilot ưu tiên gì cho Voice Calibration: (1) chỉ UI
  waveform+dialect+VAD (Option A gốc), (2) thêm Drug Pronunciation Wizard (nhanh, risk
  thấp, cải thiện Drug Recall rõ), (3) thêm cả VTLN, hay (4) full personalization
  (LoRA) ngay (nhưng defer vì resource)?"

PRIVACY OPINION:
  - Drug aliases: KHÔNG phải biometric, chỉ là text mapping → an toàn
  - VTLN warp factors: risk rất thấp (chỉ vài scalar) → KHÔNG coi là dữ liệu sinh trắc
    học theo NĐ13/2023
  - LoRA: vẫn như trước, coi là sensitive/biometric-derived
```

### Resolution
6/6 AI (5 trước + Grok review v2) đồng ý: mở rộng FID-VN-013 v1 = Option A (2.1-2.3) +
Drug Pronunciation Wizard (2.4) + VTLN (2.5, có gate AC-013). Chỉ LoRA (Section 6) tiếp
tục defer → Phase 1 Research Track. Đã cập nhật `fids/FID-VN-013.md` v2 + PA-015 với
Q1/Q2/Q3 cho Andy.

---

## ADDENDUM 2 (2026-06-10) — ChatGPT + AI #5 review của FID-VN-013 v2

### ChatGPT review

```
Đánh giá: decomposition v2 đúng hướng — phân thành 4 layer rõ ràng:
  Layer 1: UX Calibration (waveform, dialect, pause)       = Section 2.1-2.3
  Layer 2: Signal Calibration (VTLN)                       = Section 2.5
  Layer 3: Clinical Vocabulary Calibration (drug wizard)   = Section 2.4
  Layer 4: Model Personalization (LoRA)                    = Section 6, defer

Drug Pronunciation Enrollment = ROI cao nhất trong toàn bộ đề xuất — giải quyết đúng
  bài toán "ASR hiểu sai thuốc" (am lo đi pin → am lo đi phim), không cần GPU/train/
  LoRA/lưu audio dài hạn.

ĐIỂM CẦN SỬA: KHÔNG nên tạo alias dictionary riêng 100% theo từng BS (100 BS = 100
  dictionaries, khó maintain). Đề xuất kiến trúc 2 tầng:
    Drug Dictionary → Global Alias Layer → Doctor Override Layer → NER
  90% alias học được nên ở tầng GLOBAL (chia sẻ toàn hệ thống), chỉ giữ doctor-level
  override cho case cực đặc biệt.

VTLN: thận trọng — PhoWhisper là Transformer đã train trên hàng trăm nghìn giờ, có thể
  đã có speaker invariance mạnh sẵn. Chưa có bằng chứng VTLN+Whisper giảm WER đáng kể
  cho tiếng Việt y tế → "đáng nghiên cứu nhưng chưa đáng đưa vào roadmap chính".

LoRA: đồng ý defer — MediVoice chưa biết lỗi do speaker/domain/vocab/accent, thêm LoRA
  lúc này sẽ làm nhiễu phân tích root cause.

ĐỀ XUẤT CHIA NHỎ:
  FID-VN-013A: Voice UX Calibration (waveform/pause/dialect/quality) — 1-2 ngày
  FID-VN-013B: Clinical Vocabulary Calibration (drug wizard, 15-20 thuốc/specialty)
                — 2-4 ngày — ROI cao nhất
  FID-VN-013C (Research): VTLN experiment — SAU TRAIN-001, kill nếu cải thiện <2-3% WER
  FID-VN-013D (Future): LoRA — chỉ mở khi TRAIN-001 done + speaker variance proven
```

### AI #5 review

```
Xác nhận decomposition 3 lớp (VTLN / Drug wizard / LoRA) đúng chuẩn ASR industry +
  đúng với DVP Layer 3.

VTLN: risk cực thấp về pháp lý — chỉ 1-2 scalar, KHÔNG dùng để phân biệt BS A vs BS B,
  KHÔNG phải dữ liệu sinh trắc học theo NĐ13/2023, không cần consent riêng. Nên đưa
  vào FID-VN-013 v1.1 / Phase 0.5 — không ảnh hưởng TRAIN-001/pipeline/GPU.

Drug wizard: "win lớn", risk ~0 về pháp lý (text normalization, không phải biometric).
  Có thể mở rộng FID-VN-012 hoặc tách FID-VN-014.

LoRA: giữ Phase 1 Research Track — đồng ý 100% với 5-AI trước.

TỔNG HỢP — FID-VN-013 v1 NÊN GỒM:
  1. Visualization Layer (Option A): waveform, VAD, dialect, speaking rate
  2. Calibration Layer (low-risk): drug pronunciation wizard + alias auto-confirm
  3. Acoustic Normalization Layer (low-risk): VTLN warp factor (nếu research xong)
  KHÔNG GỒM: LoRA hoặc bất kỳ neural personalization theo doctor_id nào
```

### Resolution 2
8/8 AI (6 trước + ChatGPT + AI#5 review v2) đồng ý kiến trúc 3-layer cho FID-VN-013 v1
(Visualization + Drug Wizard + VTLN-with-gate), LoRA tiếp tục Phase 1 Research Track.

**Bổ sung mới quan trọng (ChatGPT)**: kiến trúc alias nên là `global_aliases` (chia sẻ
toàn hệ thống, 90% case) + `doctor_overrides` (case đặc biệt), KHÔNG phải 100%
per-doctor dictionary — đã thêm vào `fids/FID-VN-013.md` Section 2.4.1 như
**optional/Phase 1.5** (v1 vẫn implement `doctor_aliases` per-BS trước, vì pilot chỉ
1-2 BS chưa đủ data để promote lên global).

**VTLN — lưu ý mới (ChatGPT thận trọng hơn AI#5/Grok)**: PhoWhisper là Transformer đã
có speaker-invariance mạnh sẵn — VTLN có thể KHÔNG cải thiện đáng kể. AC-013 (POC gate,
threshold ≥3% WER giảm) trong FID-VN-013 đã capture đúng sự thận trọng này — nếu POC
fail, VTLN dừng ở mức "research note", không implement L0 integration.

**Đề xuất chia FID (ChatGPT)**: KHÔNG tách thành 013A/B/C/D riêng — giữ 1 file
FID-VN-013 với Section 2.1-2.3 (Visualization) / 2.4 (Drug Wizard) / 2.5 (VTLN, gated)
/ Section 6 (LoRA, deferred) vì cùng 1 chủ đề "Voice Calibration" và Andy dễ theo dõi
1 file hơn — nhưng GIỮ thứ tự ưu tiên implement: 2.4 (ROI cao nhất) → 2.1-2.3 → 2.5
(chỉ nếu AC-013 pass).

---

*CONS-20260610-005 | DS-VN-DEV-004 template v1.0*
