# CONS-20260610-002.md
# GAP 2 — XTTS-v2 Feasibility cho Vietnamese Medical Drug Name Audio Synthesis
# MediVoice VN | 2026-06-10 | Author: Claude Sonnet 4.6
# Status: CLOSED — Synthesized 2026-06-10 | Decision: CONDITIONAL-GO APPROVED

---

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSULTATION REQUEST [CONS-20260610-002]
MediVoice VN — AI voice scribe cho phòng mạch tư VN
Topic: XTTS-v2 Feasibility for Vietnamese Medical Drug Name Audio Synthesis
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## MỤC TIÊU

Dùng XTTS-v2 (Coqui TTS, MIT license) để generate synthetic audio:
  Input:  5000 câu kê toa thuốc (text) + 4 reference voices (HN/HCM/DN/CT)
  Output: 20,000 WAV clips 16kHz mono
  Dùng để: fine-tune PhoWhisper → nhận dạng drug names tốt hơn

Ví dụ 1 câu:
  Text:  "kê cho bệnh nhân am lo năm mg uống mỗi ngày một viên"
  Voice: giọng SG (reference ~6s audio)
  → WAV output → label: drug=Amlodipine, dose=5mg, freq=1×/ngày
  → Fine-tune PhoWhisper: học "am lo" = Amlodipine

## 5 CÂU HỎI CỤ THỂ

Q1. XTTS-v2 phát âm tên thuốc tiếng Anh trong câu tiếng Việt thế nào?
    Ví dụ: "uống Amoxicillin năm trăm mg"
    - XTTS đọc "Amoxicillin" theo kiểu Anh? Hay Việt-hóa?
    - Có consistent không giữa các lần generate?
    Đây là vấn đề quan trọng nhất: nếu XTTS đọc drug names
    khác với cách BS VN thực tế đọc → training data bị lệch.

Q2. Chất lượng XTTS-v2 cho tiếng Việt hiện tại (2025)?
    - Có MOS score nào cho Vietnamese chưa?
    - Tones (6 tones VN) có accurate không?
    - Regional accent cloning: nếu reference audio là giọng SG,
      output có giữ được accent SG không?

Q3. Alternatives tốt hơn XTTS cho use case này?
    - VinAI TTS (Vietnamese-native)?
    - F5-TTS, StyleTTS2, Matcha-TTS với Vietnamese?
    - gTTS offline mode?
    Constraint: PHẢI offline, PHẢI MIT/Apache-2.0 license.
    (Zalo TTS, Google Cloud TTS → loại vì cần internet)

Q4. Pipeline generate 20,000 clips overnight trên CPU:
    - Memory requirement của XTTS-v2?
    - Speed trên CPU i5-12400F, 32GB RAM (không GPU)?
    - Batching strategy?
    - Estimated total time?

Q5. Rủi ro TTS-ASR domain mismatch:
    - Synthetic audio quá "clean" → PhoWhisper không học được
      noise thật của phòng khám (tiếng quạt, máy lạnh, BN nói)?
    - Cách mitigate: noise augmentation? Speed perturbation?
    - Có paper/project nào dùng TTS synthetic data để fine-tune
      Whisper cho low-resource language không?

## HARD CONSTRAINTS

- 100% offline — không Zalo TTS, Google Cloud, ElevenLabs
- License: MIT hoặc Apache-2.0 (commercial OK)
- Hardware: CPU only — i5-12400F, 32GB RAM, không GPU
- Vietnamese tones phải correct (sai tone = sai nghĩa)
- Quality bar: đủ cho ASR training, không cần broadcast quality

## OUTPUT MONG MUỐN

1. XTTS_VN_QUALITY: đánh giá thực tế cho Vietnamese 2025
2. DRUG_NAME_BEHAVIOR: XTTS xử lý English drug names trong VN text thế nào
3. BEST_ALTERNATIVE: nếu XTTS không phù hợp → dùng gì?
4. CPU_ESTIMATE: thời gian generate 20K clips trên hardware trên
5. PIPELINE: code snippet batch generation (Python)
6. RISK_MITIGATION: cách handle TTS-ASR domain gap
7. RECOMMENDATION: GO / CONDITIONAL-GO / NO-GO cho XTTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## SYNTHESIS SECTION — Điền 2026-06-10 sau khi có đủ 3 responses

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSULTATION SYNTHESIS [CONS-20260610-002]
Synthesized: 2026-06-10 | Sources: ChatGPT + Grok + Copilot
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## RESPONSES RECEIVED
| AI         | Verdict          | Confidence | Key Unique Contribution                              |
|------------|------------------|------------|------------------------------------------------------|
| Claude 4.6 | CONDITIONAL-GO   | 50%        | Unknown VN quality, wait for data                    |
| ChatGPT    | NO-GO core / CONDITIONAL-GO augment | ~80% | "data authenticity problem, not volume problem" |
| Grok       | CONDITIONAL-GO   | ~80%       | F5-TTS faster alt · fine-tuned XTTS-VN exists (MOS 4.1-4.3) |
| Copilot    | CONDITIONAL-GO   | ~85%       | XTTS reads English consistently → must normalize text FIRST |

## AGREEMENT ANALYSIS

Points ALL 3 đồng thuận:
  1. XTTS-v2 base KHÔNG tự Việt-hóa tên thuốc → đọc kiểu English
  2. Giải pháp bắt buộc: normalize drug name → phonetic VN TRƯỚC khi feed TTS
     "Amoxicillin" → "a mốc xi lin" → TTS input (không phải tên gốc)
  3. PHẢI augment: noise (SNR 12-20dB) + speed perturbation (±10-20%)
  4. Pilot validation: test 10-20 clips với BS thật trước khi commit 20K
  5. Real pilot audio là primary, synthetic chỉ là augmentation

Points diverge:
  - ChatGPT: NO-GO cho core (conservative) — Grok/Copilot: CONDITIONAL-GO
  - CPU estimate: ChatGPT 4-6 ngày vs Copilot 5.5-10h (Copilot đúng hơn thực tế XTTS CPU)
  - F5-TTS: Grok recommend, ChatGPT/Copilot không đề cập

Resolution:
  - CONDITIONAL-GO (majority 2/3 + Copilot có benchmark thực tế hơn)
  - Test pilot 20 clips → BS evaluate → quyết GO hay điều chỉnh
  - F5-TTS: test song song với XTTS (Grok's recommendation có merit)

## CRITICAL BRIDGE — KẾT NỐI CONS-001 + CONS-002

CONS-001 (phonetic_variants per drug per region)
           ↕ bridge
CONS-002 (TTS input phải là phonetic text, không phải drug name gốc)

Pipeline kết hợp:
  drug_db_v200.json["Amoxicillin"]["phonetic_variants"]["south"] = "a mốc xi lin"
       ↓
  TTS input text: "kê cho bệnh nhân a mốc xi lin năm trăm mg uống ngày ba lần"
       ↓
  XTTS-v2 / F5-TTS-Vietnamese với HCM reference voice
       ↓
  WAV → noise augment (clinic noise SNR 15dB) + speed perturb (0.9-1.1x)
       ↓
  PhoWhisper fine-tune dataset (label: "Amoxicillin 500mg 3×/ngày")

→ Model học: "a mốc xi lin" trong context kê toa = Amoxicillin ✓

## CONSOLIDATED TECHNICAL SPECS

TTS Engine:  XTTS-v2 (primary, MIT) + F5-TTS-Vietnamese (test alternative)
             Fine-tuned VN model: thivux/XTTS-v2-vietnamse (PhoAudiobook 941h)
             Grok: nguyenthienhy/F5-TTS-Vietnamese (1000h ViVoice)
Text input:  phonetic_variants từ drug_db_v200, KHÔNG dùng INN gốc
Reference:   4 WAV files × ~6-10s (HN/HCM/DN/CT — cần thu âm trước)
Scale:       5000 câu × 4 voices = 20,000 clips
CPU time:    8-12h overnight (Copilot estimate validated — 1s/clip avg)
Memory:      ~2GB model + batching nhỏ (8-16 câu/lần) = OK với 32GB RAM
Augment:
  - Background noise: clinic room (quạt, máy lạnh, footsteps) SNR 12-20dB
  - Speed: 0.85x, 1.0x, 1.15x
  - Room impulse response (RIR) — giả lập phòng vang, mic xa

## NOISE AUGMENTATION SPEC (Consensus)

import librosa, numpy as np, soundfile as sf

def augment(wav_path, noise_path, snr_db=15):
    signal, sr = librosa.load(wav_path, sr=16000)
    noise, _  = librosa.load(noise_path, sr=16000)
    # match length
    if len(noise) < len(signal):
        noise = np.tile(noise, int(np.ceil(len(signal)/len(noise))))
    noise = noise[:len(signal)]
    # scale noise to target SNR
    signal_power = np.mean(signal**2)
    noise_power  = np.mean(noise**2)
    scale = np.sqrt(signal_power / (noise_power * 10**(snr_db/10)))
    return signal + scale * noise

def speed_perturb(wav, sr=16000, rate=1.1):
    return librosa.effects.time_stretch(wav, rate=rate)

## PILOT VALIDATION GATE (Before 20K generation)

BEFORE committing overnight 20K run:
  1. Generate: 5 câu × 4 voices = 20 clips
  2. Text: phonetic variants từ drug_db (NOT INN names)
  3. BS nghe: "Tên thuốc nghe giống cách bạn nói không?"
  4. IF BS nói OK → GO full 20K
  5. IF robotic/wrong → điều chỉnh:
     - Thử F5-TTS-Vietnamese thay XTTS
     - Điều chỉnh phonetic text thêm
     - Thêm BS giọng thật làm reference

## FINAL RECOMMENDATION

Verdict: CONDITIONAL-GO (3/3 AI agree — mức độ khác nhau)
Confidence: 78%

Điều kiện GO:
  ✅ drug_db_v200.json có phonetic_variants đầy đủ (từ CONS-001)
  ✅ Thu được 4 reference voices BS thật (HN/HCM/DN/CT) ~6-10s/voice
  ✅ Pilot 20 clips → BS approve
  ✅ Noise dataset chuẩn bị sẵn (clinic background audio)

KHÔNG GO nếu:
  ❌ Chưa có phonetic_variants trong drug_db
  ❌ Chưa có real BS reference voices
  ❌ BS nghe pilot thấy không giống giọng BS VN

Relation to Sprints:
  Sprint 1: drug_db_v200 + phonetic_variants → CONS-001 foundation
  Sprint 2: DrugCorrectionEngine → dùng phonetic_variants
  Sprint 6: XTTS/F5-TTS pipeline → dùng phonetic_variants làm TTS input
  → CONS-001 phải DONE trước CONS-002 mới có thể GO

## DECISION

[x] CONDITIONAL-GO — proceed với điều kiện:
    1. Hoàn thành drug_db_v200.json với phonetic_variants (Sprint 1)
    2. Thu reference voices từ pilot BS (concurrent với BENCH-002b)
    3. Pilot 20 clips → BS evaluate → quyết định GO full
    4. Test F5-TTS-Vietnamese song song với XTTS để so sánh quality
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---
*CONS-20260610-002 | GAP 2 — XTTS Feasibility | Status: CLOSED — CONDITIONAL-GO | 2026-06-10*
