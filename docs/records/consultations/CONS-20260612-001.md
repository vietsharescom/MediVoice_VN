# CONSULTATION REQUEST [CONS-20260612-001]
# MediVoice VN — Multi-AI Consultation
# Generated: 2026-06-12 | SES-20260612c | Liên quan: FID-VN-019 v2 (CT-042)

---

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSULTATION REQUEST [CONS-20260612-001]
From: Claude Sonnet 4.6 | MediVoice VN | SES-20260612c
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## PROJECT CONTEXT
Product: MediVoice VN — AI voice scribe cho phòng mạch tư VN
  BS nói → PhoWhisper ASR → NER → Mẫu 15/BV-01 → BS approve → PDF
Stack: Python 3.10 / FastAPI / PhoWhisper-medium / SQLite / Fernet / PWA
Constraints:
  - 100% offline (NĐ13/2023: data tại VN)
  - NOT SaMD (chỉ transcription + form fill, không chẩn đoán)
  - BS phải approve mọi record (Luật KCB 2023 Điều 62)
  - L0→L10 pipeline FROZEN — thay đổi chỉ qua FID + Andy approve
Phase: Phase 0 pilot — phòng khám Đà Nẵng (Andy) + phòng mạch Sài Gòn (BS partner)
ISO: ISO/IEC 42001:2023 + ISO 9001:2015

## QUESTION
Module `src/core/l1b_drug_correct.py` (Layer 1b) match tên thuốc tiếng Anh từ
transcript ASR (PhoWhisper) bằng exact-match alias_map xây từ `drug_db_v200.json`
(`brands`, `name_variants`, `phonetic_variants` đã curate theo 3 vùng miền VN).

Vấn đề: BS Việt phát âm tên thuốc tiếng Anh (vd "Metronidazole", "Theophylline",
"Ciprofloxacin") theo cách bị ảnh hưởng bởi âm vị tiếng Việt (L1 transfer), và
PhoWhisper transcribe ra chữ Việt theo cách BS phát âm — KHÔNG khớp với
`phonetic_variants` đã curate (chỉ ghi 1-2 cách viết/biến thể vùng miền).

Chúng tôi đề xuất hàm `_phonological_variants(alias: str) -> set[str]` sinh thêm
biến thể chính tả cho mỗi alias hiện có (brands/name_variants/phonetic_variants),
dựa trên 4 nhóm quy tắc ngữ âm học đối chiếu Việt-Anh (chi tiết Option A dưới).
Câu hỏi: **4 nhóm quy tắc này có đủ/đúng để cover phần lớn các biến thể phát âm
sai thực tế của BS Việt khi đọc tên thuốc tiếng Anh không? Có nhóm hiện tượng quan
trọng nào bị thiếu, hoặc nhóm nào trong 4 nhóm là rủi ro false-positive cao cần bỏ/
sửa?**

## OPTIONS EVALUATED

### Option A: 4 nhóm quy tắc phonological (đề xuất hiện tại, FID-VN-019 v2)
Mô tả: Sinh biến thể alias theo 4 nhóm, áp dụng tối đa 2 âm tiết/alias (đầu+cuối):
  1. **Aspiration neutralization**: hoán đổi b↔p, d↔t, g↔k, c↔k ở đầu âm tiết
     (tiếng Việt không phân biệt âm vị bật hơi/không bật hơi như tiếng Anh)
  2. **Coda restriction**: nếu âm tiết kết thúc bằng phụ âm KHÔNG thuộc bộ 6 coda
     hợp lệ tiếng Việt `{p,t,k,c,m,n,ng,nh}` (vd "l","s","z","v","f","x","d" cuối)
     → sinh thêm biến thể RỤNG phụ âm cuối đó (vd "zol"→"zo", "mid"→"mi", "vas"→"va")
  3. **th → t/d**: âm tiết bắt đầu "th" (θ/ð không có trong tiếng Việt) → sinh biến
     thể "t"/"d" (vd "theo"→"teo"/"deo")
  4. **r/l/n merger**: hoán đổi r↔l, l↔n ở đầu âm tiết (phương ngữ Bắc Bộ "nói/lói",
     r thường → l/z/d trong giọng Việt)

  KHÔNG implement (để Phase 2 — CT-052): consonant cluster epenthesis/reduction
  (tiếng Việt không có cluster phụ âm, English "stat-in"/"pred-ni-sone" có cluster
  ở biên âm tiết) — vì combinatorial explosion cao và cần audio thật để xác định
  pattern cụ thể trước khi thiết kế.

Pros:
  - Dựa trên literature về Vietnamese-English contrastive phonology (xem nguồn dưới)
  - Exact-match only (Layer 1, confidence 1.0) — không thêm fuzzy logic mới
  - Ambiguity guard: collision với INN khác → SKIP cả 2, không overwrite
  - Giới hạn 2 âm tiết/alias + min-length filter (<3 ký tự) → kiểm soát kích thước
    alias_map (~155 thuốc × ~10 alias × tối đa ~8 biến thể/alias ≈ +10-12K key)
Cons:
  - Rule 2 (coda restriction) có thể sinh nhiều biến thể ngắn, rủi ro trùng từ
    tiếng Việt phổ biến (false positive trên câu lâm sàng bình thường)
  - Rule 4 (r/l/n) là 3-way merger (r↔l VÀ l↔n) — có thể tạo biến thể không thực tế
    nếu áp dụng đồng thời cả 2 cặp trên cùng âm tiết
  - Chưa có audio thật để validate — chỉ dựa trên literature + quan sát ban đầu
    của Andy (p/b)
Risks: False-positive trên Layer 1 exact-match (ảnh hưởng trực tiếp đến output BS
  thấy) — kiểm soát bằng A/B benchmark `tools/bench_002b.py` (57 clips thật) TRƯỚC
  khi merge, yêu cầu Drug Precision không giảm >5pp
Effort: Medium (~90-120 LOC + ~60 LOC tests) | Timeline: 1 session sau approve

### Option B: KHÔNG sinh biến thể tự động — chỉ mở rộng `phonetic_variants` curated
  thủ công trong `drug_db_v200.json` dựa trên audio thật (BENCH-002b) từng case
Mô tả: Thay vì thuật toán tổng quát, mỗi khi phát hiện garble cụ thể từ audio thật
  (vd CT-027's Ciprofloxacin "si pô lo siêu âm si"), thêm 1 entry cụ thể vào
  `phonetic_variants` (đã có cơ chế này, CT-051 vừa unlock window 5-6 từ)
Pros:
  - Zero risk false-positive mới — chỉ thêm alias đã verify từ audio thật
  - Không cần FID/thay đổi code L1b — chỉ sửa data JSON (KHÔNG FROZEN)
  - Curator (Andy/Claude) review từng entry trước khi thêm
Cons:
  - Chậm — mỗi pattern garble cần audio thật trước khi fix được
  - Không scale — 155 thuốc × nhiều giọng miền × nhiều kiểu garble → số lượng case
    cần curate thủ công rất lớn
  - Không giải quyết được garble pattern CHƯA gặp trong audio đã thu (pilot mới có
    ít audio)
Risks: Recall thấp kéo dài cho đến khi đủ audio pilot (KPI pilot: 50-100h audio,
  hiện chưa đạt)
Effort: Low (per-case, không cần FID) | Timeline: ongoing, phụ thuộc audio pilot

## HARD CONSTRAINTS (KHÔNG thể vi phạm)
- Pipeline L0→L10 FROZEN — Option A phải qua FID + Andy approve + A/B benchmark
  trước merge (đã lập FID-VN-019 v2)
- Drug Recall không được GIẢM so với baseline 55.6% (BENCH-002b), Drug Precision
  không giảm >5pp — nếu Option A không đạt, phải revert
- KHÔNG sửa INN gốc trong alias_map (chỉ alias/brand/variant)
- 958/958 unit tests phải PASS + bandit 0 HIGH sau thay đổi

## CLAUDE'S CURRENT ANALYSIS
Lean toward: Option A (với scope đã giới hạn — bỏ rule cluster epenthesis sang
Phase 2/CT-052)
Confidence: 65%
Main reason: Option A có cơ sở literature rõ ràng cho 4/5 nhóm hiện tượng phổ biến
  nhất (aspiration, coda restriction, th-substitution, r/l/n merger), và có guard
  rails (ambiguity guard, A/B benchmark mandatory, exact-match only) để giới hạn
  blast radius nếu sai.
Main uncertainty:
  1. Rule 2 (coda restriction/drop) có vẻ là rule "mạnh" nhất về số lượng biến thể
     sinh ra nhưng cũng dễ tạo false-positive nhất (rụng phụ âm cuối tạo ra âm
     tiết ngắn, dễ trùng từ tiếng Việt thông dụng) — không chắc filter <3 ký tự đủ
     để kiểm soát.
  2. Rule 4 (r/l/n) — không chắc nên áp dụng r↔l VÀ l↔n đồng thời (3-way) hay nên
     tách 2 cặp riêng và chỉ chọn 1 cặp phù hợp với vùng miền (Bắc: l↔n; Nam: r→l/z).
  3. Không có cách nào để Claude tự verify "tỷ lệ garble thực tế" — phụ thuộc hoàn
     toàn vào A/B benchmark sau khi code, có rủi ro phải revert nếu sai (1 session
     "lãng phí" nếu Drug Precision giảm quá nhiều).

## WHAT I NEED FROM YOU
Please provide:
1. Your recommendation: A / B / other — with reasoning
2. Confidence level: X%
3. Key risks or considerations I may have missed (đặc biệt: linguistic accuracy
   của 4 nhóm quy tắc — có nhóm hiện tượng quan trọng nào bị bỏ sót không? Có
   nghiên cứu cụ thể nào về garbling tên thuốc tiếng Anh BỞI người Việt — khác với
   English-learner pronunciation nói chung — mà nên tham khảo?)
4. Is this an owner (Andy) decision vs technical decision? Why?
5. If owner decision: what specific question should Andy answer?
6. Cụ thể cho rule 4 (r/l/n): nên áp dụng đồng thời r↔l↔n (3-way) hay tách theo
   vùng miền (Bắc: l↔n; Nam/Trung: r→l hoặc r→z)?

Return format:
  RECOMMENDATION: [A/B/other]
  CONFIDENCE: [X%]
  KEY RISKS: [list]
  OWNER DECISION: [yes/no + why]
  OWNER QUESTION: [if yes, what to ask]
  RULE-4 RECOMMENDATION: [3-way / split-by-region / other]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

## NGUỒN ĐÃ DÙNG (research 2026-06-12, đính kèm để AI khác đối chiếu)

- [Interlanguage Phonology and the Pronunciation of English Final Consonant Clusters
  by Native Speakers of Vietnamese](https://www.academia.edu/3465003/Interlanguage_Phonology_and_the_Pronunciation_of_English_Final_Consonant_Clusters_by_Native_Speakers_of_Vietnamese)
- [Influence of Vietnamese Pronunciation on the Production of English Final Consonant
  Sounds](https://www.academia.edu/31932060/Influence_of_Vietnamese_Pronunciation_on_the_Production_of_English_Final_Consonant_Sounds)
- [Common mistakes in pronouncing English consonant clusters: A case study of
  Vietnamese learners (CTU Journal)](https://ctujs.ctu.edu.vn/index.php/ctujs/article/view/448)
- [Contrastive Analysis of Consonants in English and Vietnamese
  (ResearchGate)](https://www.researchgate.net/publication/352243293_Contrastive_Analysis_of_Consonants_in_English_and_Vietnamese)
- [The Adaptation of French Consonant Clusters in Vietnamese Phonology: An OT Account
  (J. Univ. Lang.)](https://www.sejongjul.org/archive/view_article?pid=jul-18-1-69)
- [Vietnamese phonology (Wikipedia)](https://en.wikipedia.org/wiki/Vietnamese_phonology)

## FID liên quan
- `fids/FID-VN-019.md` (DRAFT v2) — chi tiết đầy đủ thuật toán, acceptance criteria,
  risks, tests
- `docs/records/BACKLOG.md` — CT-042 (chờ approve), CT-052 (Phase 2, cluster
  epenthesis)

---

## SYNTHESIS — chạy tự động qua `scripts/orchestrator.py consult` (2026-06-12)

Andy hỏi "Claude có tư vấn tự động sao không dùng" — đã chạy thử `orchestrator.py`
với 4 provider đã có API key trong `.streamlit/secrets.toml`. Kết quả:

| AI | Trạng thái | Recommendation | Confidence |
|---|---|---|---|
| Groq/LLaMA-3.3-70B | ✅ trả lời | A | 80% |
| OpenAI/GPT-4o-mini | ❌ HTTP 429 — hết quota (billing) | — | — |
| xAI/Grok-3 | ❌ HTTP 403 — team chưa có credit | — | — |
| OpenRouter/LLaMA-3.3-70B-free | ❌ HTTP 429 — rate-limited (free tier) | — | — |

Chỉ Groq trả lời được (3/4 provider lỗi do billing/quota, KHÔNG phải lỗi code).
Raw output: `docs/records/consultations/ORCH-CONSULT-20260612-202447.json`

**Groq/LLaMA-3.3-70B response:**
```
RECOMMENDATION: A
CONFIDENCE: 80%
KEY RISKS:
 - False-positive cao do quy tắc không chính xác
 - Thiếu dữ liệu thực tế để kiểm tra hiệu quả của quy tắc
 - Có thể không bao phủ được tất cả các biến thể phát âm sai
RULE-4 RECOMMENDATION: split-by-region
```
Giải thích thêm: khuyến nghị Option A (thuật toán tổng quát) nhưng với điều chỉnh —
áp dụng rule 4 (r/l/n) THEO VÙNG MIỀN (Bắc: l↔n; Nam/Trung: r→l hoặc r→z) thay vì
3-way đồng thời, và kiểm tra hiệu quả trên dữ liệu thực tế (= A/B benchmark đã có
trong FID-VN-019 v2 acceptance criteria).

## FINAL RECOMMENDATION (Claude, sau khi có 1/4 response)

- **Option A** (4 nhóm quy tắc, FID-VN-019 v2) — giữ, đồng thuận với Claude's lean
  ban đầu (65% → 75% sau khi có ý kiến độc lập đồng ý).
- **Rule 4 (r/l/n)**: chuyển từ "3-way đồng thời" → **split-by-region**, theo cả
  Groq VÀ phân tích ban đầu của Claude (FID-VN-019 v2 cần update: rule 4 nên đọc
  `region` field trong `phonetic_variants` của `drug_db_v200.json` — chỉ sinh
  l↔n cho alias vùng `north`, r→l/r→z cho alias vùng `central`/`south`).
- Risk "thiếu dữ liệu thực tế" — đã có sẵn trong FID-VN-019 v2 (A/B benchmark
  `tools/bench_002b.py` mandatory trước merge) → không cần thay đổi thêm.

## OWNER DECISION NEEDED?

NO — kết quả 1/4 AI đồng thuận với hướng đã chọn (Option A + A/B benchmark gate),
chỉ cần 1 sửa nhỏ trong FID (rule 4 → split-by-region theo `region` field, đã có
sẵn trong data). Claude sẽ cập nhật FID-VN-019 v2 rule 4 theo hướng này.

**Lưu ý cho Andy**: 3/4 AI provider trong `.streamlit/secrets.toml` đang KHÔNG hoạt
động được (OpenAI hết quota, xAI/Grok chưa có credit, OpenRouter free-tier rate-limit)
— nếu cần multi-AI consensus thật (không chỉ Groq), cần top-up billing cho
OpenAI/xAI hoặc đợi OpenRouter rate-limit reset. Đây là lý do Claude thường tạo
CONS-*.md để Andy paste tay vào ChatGPT web (không qua API, không bị quota) — tool
tự động `orchestrator.py consult` CÓ THỂ DÙNG nhưng hiện chỉ 1/4 provider hoạt động.

---

## ROUND 2 — ChatGPT review (Andy paste tay, 2026-06-12)

Andy paste thêm review từ ChatGPT (đọc toàn bộ CONS-20260612-001 + FID-VN-019 v2).

**Kết luận ngắn của ChatGPT:**
```
RECOMMENDATION: A modified
CONFIDENCE: 88%
RULE-4: split-by-region
OWNER DECISION: NO
```

**Phản biện chính** (tóm tắt — full text Andy đã paste trong chat, không lưu lại
nguyên văn ở đây để tránh trùng lặp):

1. **Reframe vấn đề**: L1b KHÔNG còn là "phonological matching" thuần — vì ASR đã
   transcribe âm thanh thành CHUỖI CHỮ VIẾT TIẾNG VIỆT rồi. Đây là **"Vietnamese
   Orthography Matching"** — bài toán match 2 chuỗi chữ Việt, không phải match
   2 cách phát âm.
2. **Rule 1 (b/p, d/t, g/k/c)** — ✅ giữ, ROI cao nhất, false-positive thấp
   (vd "prednisone"↔"brednisone").
3. **Rule 2 (coda drop)** — ⚠️ NGUY HIỂM NHẤT, dự đoán "80% false-positive sẽ đến
   từ Rule 2". Lý do: các token ngắn sinh ra ("va", "zo", "mi", "te"...) trùng rất
   nhiều với từ tiếng Việt phổ biến. **Đề xuất**: KHÔNG áp dụng cho alias 1 âm tiết
   — chỉ áp dụng trong context multi-syllable (vd "metro ni da zol" → "...da zo" OK,
   nhưng KHÔNG thêm key đứng riêng "zol"→"zo").
4. **Rule 3 (th→t/d)** — ✅ giữ, "lỗi kinh điển của người Việt".
5. **Rule 4 (r/l/n)** — ✅ đồng ý split-by-region (giống Groq), PHẢN ĐỐI hoàn toàn
   3-way (đã sửa ở FID-VN-019 v2 rồi — không cần đổi thêm).
6. **THIẾU — Vowel normalization** (đánh giá QUAN TRỌNG HƠN Rule 4): biến đổi
   o/ô/ơ, i/inh, en/eng, an/ang (vd "metformin" → "mét pho min"/"mét phọc
   min"/"mét phô min"). Đề xuất Phase 2 riêng.
7. **THIẾU — Stress relocation / Syllable insertion (cluster breaking)**: hiện
   tượng BS Việt thêm âm tiết đệm khi gặp cluster tiếng Anh (vd "statin"→"sờ ta
   tin", "steroid"→"xì tê rôi", "clopidogrel"→"cờ lô pi đô gờ ren"). Đánh giá
   **hiệu quả lớn hơn Rule 4**. Claude đã defer nhóm này sang CT-052 (đồng ý defer).
8. **Đề xuất kiến trúc dài hạn**: thay vì `_phonological_variants()` enumerate biến
   thể (sẽ "nổ" theo cấp số nhân khi 155→300→1000 thuốc), nên xây
   **Vietnamese Medical Phonetic Encoder** — kiểu Soundex/Metaphone/Double Metaphone
   nhưng cho tên thuốc tiếng Việt: encode alias → canonical code (vd "mét phọc
   min"/"mét phô min"/"mét pho min" đều → `MTFRMN`), match qua code thay vì
   enumerate. Đề xuất **CT-053 — Vietnamese Medical Phonetic Encoder**.

**Review verdict**: APPROVE WITH CHANGES
- ✅ Rule 1 giữ
- ✅ Rule 3 giữ
- ✅ Rule 4 → split-by-region (đã làm)
- ⚠️ Rule 2 → chỉ multi-syllable alias, KHÔNG token đơn
- ⚠️ Thêm CT-053 vào BACKLOG (kiến trúc dài hạn)

## CLAUDE'S FINAL SYNTHESIS (sau ROUND 2)

Đồng ý với ChatGPT trên cả 5 điểm. Cập nhật:

- **FID-VN-019 → v3**: Rule 2 thêm constraint "chỉ áp dụng khi alias có ≥2 âm tiết
  (KHÔNG sinh alias_map key mới là 1 âm tiết đơn, dù đã pass min-length filter
  3 ký tự)" — giảm rủi ro R-PHON-01 thêm 1 lớp nữa theo dự đoán 80% FP của ChatGPT.
- **Vowel normalization + cluster/syllable-insertion**: KHÔNG thêm vào FID-VN-019
  v3 (đã đủ phức tạp, 4 rule groups). Gộp 2 hiện tượng này (#6 + #7 của ChatGPT)
  + CT-052 (cluster epenthesis) vào **CT-053 — Vietnamese Medical Phonetic Encoder**
  như 1 hướng kiến trúc Phase 2 dài hạn (Soundex-style), thay vì tiếp tục mở rộng
  `_phonological_variants()` enumerate-based (đúng như ChatGPT cảnh báo sẽ "nổ" khi
  scale 155→1000 thuốc).
- **CT-052 cũ** → merge vào CT-053 (CT-053 supersedes CT-052, phạm vi rộng hơn:
  cluster breaking + syllable insertion + vowel normalization, tất cả đều hướng
  tới phonetic-encoding chung thay vì enumerate riêng từng nhóm).

## OWNER DECISION NEEDED?

NO (ChatGPT cũng kết luận NO). FID-VN-019 v3 (Rule 2 thu hẹp thêm) + CT-053 mới
trong BACKLOG — Claude tự thực hiện 2 thay đổi này, vẫn CHỜ ANDY APPROVE FID-VN-019
v3 trước khi code (touches FROZEN L1b, không đổi).

## ROUND 3 — Grok review (Andy paste tay, 2026-06-12)

```
RECOMMENDATION: A (với một số tinh chỉnh nhỏ)
CONFIDENCE: 78%
OWNER DECISION: No (technical decision)
RULE-4 RECOMMENDATION: split-by-region
```

**Đồng thuận với Round 1 (Groq) + Round 2 (ChatGPT)**:
- Option A + split-by-region cho rule 4 (Bắc: l↔n + một phần r→l; Nam/Trung:
  r→l hoặc r→z) — đọc field `region` trong `drug_db_v200.json`, kết hợp dialect
  detection đã có (FID-VN-013).
- Rule 2 (coda drop) là nguồn FP lớn nhất — "mid"→"mi", "zol"→"zo" dễ trùng từ
  tiếng Việt thông dụng. Min-length <3 ký tự CHƯA ĐỦ.
- Thiếu vowel insertion/epenthesis + cluster simplification (statin→"sờ tát tin",
  prednisone→"pờ rét ni xôn") — pattern rất phổ biến, → Phase 2 (CT-053).

**Điểm MỚI so với Round 1+2 — guard mạnh hơn cho Rule 2**:
- Chỉ áp dụng coda-drop nếu coda là **voiced obstruent hoặc cluster** (vd "z","v",
  "d" cuối) — **KHÔNG drop nasal/plosive đơn giản** (m,n,ng,p,t,k — vốn đã hợp lệ
  coda tiếng Việt nên rule 2 hiện tại đã loại trừ, nhưng Grok nhấn mạnh thêm: cũng
  KHÔNG drop "l" nếu "l" theo sau là context dễ nhầm — case-by-case).
- **Blacklist từ tiếng Việt phổ biến** cho biến thể sinh ra (vd "mi", "va", "teo",
  "te", "no", "lo"...) — KHÔNG thêm vào alias_map dù pass min-length + multi-syllable
  constraint, nếu token kết quả nằm trong danh sách từ tiếng Việt thông dụng.
- Gợi ý hybrid: kết hợp CT-053 với Drug Pronunciation Wizard (FID-VN-013/014) — BS
  đọc → ASR + rule-based variant → confirm → enrich `phonetic_variants` tự động.

## CLAUDE'S FINAL SYNTHESIS (sau 3 rounds — Groq 80%, ChatGPT 88%, Grok 78%)

**Đồng thuận 3/3**: Option A, rule 4 split-by-region, owner decision = NO (technical).

**Cập nhật FID-VN-019 → v3 (bổ sung guard Rule 2 từ Grok, BÊN CẠNH constraint
"≥2 âm tiết" từ ChatGPT)**:
- Rule 2 chỉ sinh biến thể khi coda bị rụng thuộc nhóm **voiced obstruent**
  (`z, v, d, đ` cuối — không phải `l`; "l" giữ ở rule 2 cũ vì case "-zole"/"-mide"
  rất phổ biến trong tên thuốc và đã có ambiguity guard riêng)
- **Blacklist từ tiếng Việt phổ biến** áp dụng cho TẤT CẢ biến thể sinh ra (rule
  1-4), không chỉ rule 2 — implement như 1 set hardcode nhỏ (~30-50 từ phổ biến
  trong câu lâm sàng: "có", "không", "đau", "mệt", "về", "với", "này"... + các
  token 2-ký-tự nguy hiểm Grok nêu: "mi","va","teo","te","no","lo")
- CT-053 (đã thêm vào BACKLOG) ghi nhận thêm gợi ý hybrid với Drug Pronunciation
  Wizard (FID-VN-013/014)

3 AI độc lập đều hội tụ về cùng kết luận (Option A + split-by-region + Rule 2
guard mạnh hơn + defer cluster/vowel sang Phase 2) → confidence tổng hợp tăng từ
65% (Claude ban đầu) lên **~85%**. FID-VN-019 v3 đã cập nhật đầy đủ, sẵn sàng cho
Andy approve.

---

*Generated by Claude Sonnet 4.6 | MediVoice VN | 2026-06-12*
*Round 1: tự động qua `orchestrator.py consult` (Groq, 20:24). Round 2: ChatGPT
review do Andy paste tay. Round 3: Grok review do Andy paste tay.*
